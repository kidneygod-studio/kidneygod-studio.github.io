# 護腎教室 kidneygod.net — 變更紀錄

回頭查詢用。每一則寫「改了什麼、為什麼這樣改、踩到什麼坑」——結果本身看
git diff 就有，值得記下來的是當時的判斷理由，以及那些不會噴錯的陷阱。

---

## 工具與產出對照（先看這張表）

改東西之前先確認你改的是「來源」還是「產出」。改到產出，下次重跑產生器
就會被蓋掉，而且不會有任何警告。

| 產出 | 由誰產生 | 真正的來源 |
|---|---|---|
| `articles/*.html`、`index.html`、`about.html`、`calc.html`、`food.html` | `build_site.py` | `knowledge_export.json`、`articles_src/*.md` |
| `cards/gi/*.png`（知識卡） | `make_gi_cards.py` | `gi/art/<id>.*` ＋ `knowledge_export.json` |
| `gi/art/*.jpg`（卡片插圖） | `import_gi_art2.py` | `Downloads/知識卡插圖/` ＋ `GI插圖_*_Prompt.txt` |
| `hero/*.jpg`（長文大圖） | `make_hero.py` | `hero_src/*.jpg`（原圖，不進版控） |
| `og/*.jpg`（分享預覽圖） | `make_og.py` | 長文用 `hero_src/`，其餘用 `Downloads/kidneygod.png` |
| `logo.png` | `make_logo.py` | `Downloads/kidneygod.png`（⚠ 預設不覆寫，要加 `--logo`） |
| `food_db.json` | `build_food_db.py` | 食藥署開放資料 CSV |
| 文章頁的「專家訪談」區塊 | `build_site.py` | `interviews.json` |
| `sw.js` 的 `VERSION` | `bump_assets.py` | 全站資產的雜湊 |

**手寫、不由產生器管的**：`shop.html`、`game.html`、`library.html`、`dash.html`。

### 改完之後

```bash
python build_site.py      # 內容有改
python bump_assets.py     # 資產有改（圖片、js、頁面）
python check_site.py      # 推之前對帳
```

`check_site.py` 沒過就別推。它查的都是不會噴錯、只會讓使用者看到舊的或錯的
東西的那一類問題。

---

## 2026-09-11（二）　修好每日新知：它從搬到 macOS 之後就沒發佈成功過

### 病灶

`check_site.py` 因為三個素材來源不存在而回傳 1；`publish_daily.py` 的設計是
「check_site 沒過就不 push（不發佈壞掉的站）」，於是**每天跑、每天失敗**。
今天 07:07 的 log 最後一行就是 `Website publish returned exit 2`。

而那三項的內容是：`make_logo.py`、`make_og.py`、`import_gi_art2.py` 指向
舊 Windows 機的 `C:\Users\user\Downloads\...`。**網站本身完全正常**——線上
用的是已經壓好、已經進版控的產出，那些來源只有「重新產生」時才需要。

一句話：**一個不影響線上的檢查，擋掉了整條發佈管線。**

### 修法一：check_site 分成「擋發佈」與「只提醒」

新增 `WARN_ONLY`，目前只有「產生器的來源檔」在裡面。判準寫在檔頭：
**這一項出問題時，線上的站是不是仍然正確。** 素材不見了代表「以後重新
產生不了」，不是「使用者現在看到錯的東西」——後者才是這支腳本存在的理由。

輸出改成 `⚠ …（僅提醒，不擋發佈）`，離開碼不受影響。資訊一點都沒少，
只是不再拿它擋發佈。

順帶修掉一個假通過：**空資料夾要當成沒有**。雲端同步會先出現空殼、檔案
才慢慢到位，只查 `exists()` 的話那段空窗期會回報成「通過」。

### 修法二：五支產生器的路徑集中到 asset_paths.py

原本五支各自寫死 `C:\Users\user\Downloads\...`，是**五個地方各壞各的**。
新增 `asset_paths.py`，解析順序：

  1. 環境變數 `KIDNEYGOD_ASSETS`（任何機器、任何平台都能覆寫）
  2. Google Drive 的預設位置（macOS 的 `~/Google Drive` 與
     `~/Library/CloudStorage/GoogleDrive-*`、Windows 的 `G:\` 都試，
     `我的雲端硬碟` 與 `My Drive` 兩種命名也都試）
  3. repo 內的 `assets_src/`（.gitignore 擋著，單機備援）

素材資料夾（和 hero_src 同一個作法）：

    ~/Google Drive/我的雲端硬碟/kidneygod_assets/
        kidneygod.png          ← logo 原圖
        知識卡插圖/            ← 第一批
        知識卡插圖/知識卡插圖2/ ← 第二批
        貓咪貼圖/

**從醫院電腦把素材丟進這裡，兩台就都能重新產生了。** 目前四項都還是空的
（舊機器的原檔沒跟著搬過來），所以 check_site 會持續提醒——但不再擋發佈。

`check_site.py` 也改成直接用 `asset_paths` 解析，不再用正規表示式去刮腳本
裡的路徑字串。舊寫法在腳本改成用變數組路徑的當下就會失效，而且失效時報的是
「找不到來源設定」，看起來像檢查壞了、不像素材不見了。

### 修法三：make_og.py 不再整支當掉，新長文的 og 會自動產生

它的 `main()` 一開始就無條件 `make_card()` 去開 logo 原圖，**不管這一頁有
沒有大圖**。所以搬機之後每加一篇長文，都得手動補一張 og——而 `check_site`
查的是「HTML 引用的本地檔案」，不查 `og:image`，漏了不會有人告訴你。

改成：沒有 logo 原圖時只產大圖版，既有的 logo 版原封不動留著。

⚠ **第一次改完跑，它一口氣改掉了 16 張既有的 og。** 原因是 `hero_file()`
優先用 `hero_src/` 的原圖、沒有才退回 `hero/`；而 `hero_src/` 不進版控，
在沒做過那些圖的機器上只找得到壓過的 1600×900——拿它再壓成 1200×630 是
**二次壓縮**，畫質默默變差。已加防護：**已經有 og、而手上只剩壓過的
`hero/` 時就不重做**。現在跑它只會動到真正缺的那些。

順帶補上 `og/legal.jpg`（`a431bed` 加法律頁時就缺，分享到 LINE 會是破圖）。
內容是既有 logo 版的複本，與 `make_card()` 的產出位元組相同（md5 已比對）。
`make_og.py` 的破圖檢查現在回報「全部存在」，離開碼 0。

### 驗證

`check_site.py` 離開碼 0、`make_og.py` 離開碼 0 且不動任何既有檔案、
五支腳本語法與路徑解析都確認過、完整跑一次 build → bump → check 沒有回歸。

---

## 2026-09-11　長文大圖的原圖改走 Google Drive 同步

### 問題：原圖散在各台機器，最後就拿不回來了

`hero_src/` 在 .gitignore 裡（原圖 26 MB，進 git 歷史就永遠拿不掉），
所以它**只存在於當初做圖的那一台機器**。作者會在醫院電腦與這台 Mac
交替工作，久了原圖就會一半在這邊、一半在那邊。

這不是假想的風險，是**已經發生過一次的事**：`cards/`（224 張）、
`gallery/`（214 張）、`gi/`（100 張）、`stickers/`（304 張）、`logo.png`
的原始素材全在舊 Windows 機器的 `Downloads\`，換到 Mac 之後一個都找不到。
那 800 多張圖現在只剩壓過的產出，重產不了——`check_site.py` 那三項
「產生器的來源檔不存在」就是這件事的殘影。

### 作法：資料夾放雲端，repo 裡放符號連結

    ~/Google Drive/我的雲端硬碟/kidneygod_hero_src/   ← 原圖實際位置
    ~/dopamine_shop/hero_src  →  上面那個          ← 符號連結

好處是三邊都不用改：原圖自動在機器之間同步、`.gitignore` 照樣擋著、
`make_hero.py` 寫死的 `SRC = ROOT / "hero_src"` 也不必動。

**在另一台機器上要做的事**（repo 本身不帶任何設定，連結是每台各自建的）：

```bash
rmdir ~/dopamine_shop/hero_src 2>/dev/null
ln -s ~/"Google Drive/我的雲端硬碟/kidneygod_hero_src" ~/dopamine_shop/hero_src
```

### 踩到的坑：.gitignore 的尾斜線擋不住符號連結

`hero_src/` 這條規則帶著尾斜線，而 **git 把符號連結當檔案、不當目錄**，
所以連結一建立，`git status` 立刻冒出 `?? hero_src`——差一點就把連結
commit 進去，而它的目標路徑每台機器都不一樣（而且含帳號 email）。

改成沒有尾斜線的 `hero_src` 就同時匹配目錄與符號連結。`dialysis/img_src`
同一個型態，一併改掉，免得哪天同樣處理透析中心的原圖時再踩一次。

驗證過：`git check-ignore` 擋得下、`git status` 乾淨、`make_hero.py`
透過連結讀得到原圖且輸出與已 commit 的那份**位元組相同**（沒有多餘變更）。

---

## 2026-09-10（五）　新長文：高血磷；指引補上 KDIGO CKD-MBD

### 長文〈磷太高，問題常常不在蛋黃，而在成分表〉

第 17 篇長文，分類「飲食護腎」，6,241 字元（目前最長，前一名是高血鉀那篇的
5,659）。刻意寫成[血鉀那篇](/articles/high-potassium-ckd.html)的姊妹作，
結構呼應，但**開宗明義講清楚兩者不一樣**：鉀是急症，可能當天心律不整；
磷不是，它的帳單好幾年後才到。這一點單獨拉成一節，因為讀者最容易把鉀的
印象套到磷上，然後往兩個相反的方向錯——要嘛過度恐慌，要嘛發現幾個月
沒事就不管了。也因為這樣，這篇**沒有「什麼時候該衝急診」那一節**
（鉀那篇有），改成強調追蹤趨勢。

兩根主軸：

1. **「哪一種磷」比「多少磷」重要。** 天然食物的磷和蛋白質結合，吸收率
   打折（動物性四到六成，植物性因植酸只有兩到四成）；加工食品的含磷
   添加物是游離的，吸收率九成以上。而**台灣的營養標示不強制標磷**，
   它只出現在成分表裡。這直接推翻「戒蛋黃牛奶」那套常見衛教——
   把吸收率打折的好食物戒掉、把接近全吸收的添加物留著，方向是反的。
2. **磷結合劑的時機。** 藥抓的是還在腸道裡的磷，不是血裡的磷，所以
   飯後補吃幾乎等於沒吃。開頭第二句病人的話就是為這段鋪的。

### 指引頁新增 KDIGO CKD-MBD（第 24 份）

長文引用了這份指引，指引頁卻沒有它，連不過去。補上之後 `UPDATES_REVIEWED`
一併改成 2026-09-10——那一行的註解就寫著「每次增修這份清單就把這行改掉」。

⚠ **出處的頁碼刻意留空。** 這一區的規矩是「年份、期刊、卷期一律以查證過的
為準，不確定的欄位寧可留空也不編」。KDIGO 官網頁面沒有登期刊卷期，所以去
抓了指引 PDF：內嵌的 InDesign 檔名是 `KISU_v7_i1_COVER.indd`，
**KISU = Kidney International Supplements、v7 i1 = 卷 7 第 1 期**，卷期
就是這樣確認的。但頁碼範圍抽不出來（內文是字型子集編碼），所以寫成
`Kidney Int Suppl 2017;7(1)` 而不是憑印象補上 `:1–59`。

官網同時證實了「2017 年是 selective update、要和 2009 年版並用」這件事，
專業重點那一欄照這個寫。

### 踩到的坑：新長文的 og 圖不會自動出現

建置完之後頁面就指向 `og/articles-high-phosphorus-ckd.jpg`，但那個檔案
不存在——分享到 LINE／Threads 會是破圖，而 **`check_site.py` 不會抓到**
（它查的是 HTML 引用的本地檔案，沒查 `og:image` 這個 meta）。

`make_og.py` 在這台機器上跑不起來，因為 logo 來源寫死成 Windows 路徑
（`C:\Users\user\Downloads\kidneygod.png`）。但沒有大圖的頁面拿到的本來
就是同一張 logo 版：全站 38 個 og 檔案雜湊完全相同。所以直接複製一份，
結果和生成器產出的位元組一致（已比對 md5 與尺寸 1200×630）。

**這一項以後每加一篇長文都會再遇到一次。** 兩個可以考慮的作法：把
`make_og.py` 的 logo 路徑改成 repo 內的相對路徑，或在 `check_site.py`
加一條「og:image 引用的檔案存不存在」。目前都沒做。

（順帶記一筆：`legal.html` 也缺 `/og/legal.jpg`，是 `a431bed` 加法律頁時
就有的既有問題，這次沒動。）

---

## 2026-09-09

### 每日新知自動發佈上線 + 新知區開放 Nature／Nature Medicine

改了什麼：
- 新增 `publish_daily.py`：每日摘要 runner 生成報告成功後自動呼叫它，跑
  `import_digests.py --write` → `build_site.py` → `bump_assets.py` →
  `check_site.py` → `git push`。以前這幾步是手動跑的，現在自動。
- `import_digests.py` 的 `ALLOWED_JOURNALS` 加入 `NATURE`、`NATURE MEDICINE`
  （JOURNAL 正規化表也補上 Nature Medicine 的別名）。每日摘要 prompt 同步
  把 Nature／Nature Medicine 加進搜尋（限臨床／轉譯型，過濾純基礎科學）。

為什麼：作者要求每日三篇一律自動上網站、不再手動審核逐日 `--only`；並希望
搜尋範圍擴及 Nature。匯入器若不同步開放 Nature，摘要挑到的 Nature 論文會被
「期刊不收」默默擋掉。

踩到的坑：
- `publish_daily.py` 刻意**不帶 --only**、改用「git 有無變動」判斷要不要 build/push。
  好處是自我修復（某天 push 失敗，隔天成功會一起補上）。但第一次跑會和先前用
  `--only` 產生的 news.json 排序不同 → 多出一個一次性對齊 commit；第二次起就穩定
  「沒有新東西不 push」。這是預期的，不是 bug。
- git push 走無人值守，設 `GIT_TERMINAL_PROMPT=0` 讓沒憑證時快速失敗、不卡互動提示。
- `check_site.py` 沒過就不 push——不發佈壞掉的站。
- **最大的坑：一開始用 `git add -A`，會把作者其他還在編輯的 WIP 一起掃上線**
  （當天正在改 dialysis/ 透析中心頁）。無人值守的自動發佈**絕不能 git add -A**。
  改成白名單 `PUBLISH_PATHS`（news.json / articles / index.html / sitemap /
  sw.js / search_index.json），且「有沒有新東西」只看 `news.json` 有無變動、
  不看整個工作區（否則作者的 WIP 會誤觸發 build/push）。

（相關：nephrology_digest 那邊因醫院網路封 Telegram，改用 ntfy 推播，見該專案紀錄）

---

## 2026-09-10（四）　護腎教室：LINE 官方帳號上線

作者開通了 `@511dxhiz`（個人／創作者類型，灰盾）。

`SOCIAL` 加一筆，同時進到頁尾的社群膠囊與 JSON-LD 的 `sameAs`
——後者是告訴搜尋引擎「這個帳號和本站是同一個實體」，
對醫療類內容的作者判定有直接影響。

網址用 `line.me/R/ti/p/%40511dxhiz`：**@ 一定要 percent-encode 成 %40**，
不然部分環境會把它吃掉。手機開會直接跳進 LINE，桌機落到網頁版帳號頁。

~~⚠ **這個 ID 我沒辦法從這台機器驗證**——`line.me` 與 `page.line.me` 都是
SSL 握手逾時（醫院網路擋的），`lin.ee/511dxhiz` 回 404 但那不算數，
短網址是另一組代碼。以作者提供的為準，請他自己點一次確認。~~

**已於 2026-09-10 晚間驗證，ID 正確。** 作者提供了 LINE 後台下載的官方 QR，
解出來是 `https://lin.ee/TdkA5PL`——短網址果然是另一組代碼（當初猜
`lin.ee/511dxhiz` 是錯的方向）。這組短網址 `curl` 得到：

```
301 → https://line.me/R/ti/p/@511dxhiz?oat_content=qr
```

**由 LINE 自己的伺服器把短網址展開成 `@511dxhiz`**，等於官方認證了這個代號，
也確認站上現用的網址指向同一個帳號。此時 `line.me` 也連得上了（回 200），
先前的 SSL 逾時是當下的網路狀況，不是永久的。

分享列本來就有 LINE（`social-plugins.line.me/lineit/share`），不用改。

頁尾五顆膠囊在 320–1280 五個寬度都確認過不溢出——這一區之前就因為
「Threads @kidney.godreborn」太寬擠爆過容器。

### 先備好的素材（在 scratchpad，未進版控）

大頭貼 640×640、封面 1080×878、圖文選單 2500×1686。

圖文選單是用 HTML 渲染再截圖，不是繪圖程式畫的——中文要真的字型排版
才好看，繪圖函式庫沒有思源黑體／宋體。1250×843 的版面配 2 倍解析度，
輸出剛好是原生 2500×1686，腳本裡有斷言擋住尺寸跑掉。
原始檔 `richmenu.html` 留著，改文案或換格子只要改一行再重跑。

### 後續：首頁最下方也放一份

`build_home()` 加一個 `line_sect`，排在 `sections` 最後——讀完整頁之後才問
要不要訂閱，比一進站就攔人合理。橫帶底色交替是照「實際排出來的順序」算的，
所以接在遊戲區（tint）後面會自動變成無 tint，作者／免責聲明那條 tail 仍是
tint，節奏沒斷。

**沒有做第二套素材**：圖檔沿用 `/line-qr.svg`，樣式沿用 `.lineqr`，
所以帳號代號改了還是只要重跑一次 `make_line_qr.py`，兩頁一起換掉。

⚠ **驗證方式和關於頁那次不一樣，因為原本那套在這一頁行不通。**
關於頁是「整頁截圖再解碼」；首頁的 QR 在很下面，headless Chrome 的
`--screenshot` 只拍視窗頂端，帶 `#line` 進去也只拍到一片空白（截到的是
捲動前的畫面）。改成把 `.lineqr img` 那幾行 CSS 原封不動搬到一張最小的
測試頁，讓 QR 直接落在視窗裡再拍、再用 OpenCV 解碼——驗的還是
「瀏覽器套用同一組 CSS 畫出來的樣子」，1x／2x 都掃得出且網址完全正確，
縮到 200px 仍可解碼、150px 解不出（和當初產 PNG 時測到的界線一致）。

### 評估過官方 QR，結論是不換

作者問要不要改用 LINE 後台下載的官方 QR（黑色與綠色兩版）。查完的結論是
**留著自己產的 SVG**，理由不是美觀：

**換的最大理由已經消失。** 原本想換是為了「代號有源頭可驗」，但解開官方 QR
的過程本身就把代號驗掉了（見上面那則更正），現用的網址確認無誤。

**官方那張反而驗不過本站的標準。** 它是 29×29（version 3，比我們的 37×37
還疏），但 **OpenCV 與 jsQR 兩個獨立解碼器都解不出原圖**——補足靜區
（原圖只有 1.6 模組，規範要求 4）也一樣。要重新取樣成 29×29 位元矩陣、
用標準方塊重繪之後才解得出來。原因是它的造型化設計：圓角定位點、
模組是分離的圓點、中間壓著 LINE 字樣。手機相機通常掃得動這種造型碼，
但「兩個解碼器都讀不到」這件事沒辦法用本站現有的驗證流程放行。

**唯一值得抄的是 `?oat_content=qr`** ——官方短網址展開後帶的參數，LINE 後台
會據此分辨「這個好友是掃 QR 來的」。要做流入來源統計時再加，加了就得重跑
`make_line_qr.py`。

⚠ 綠色那版不要直接上站：QR 卡片的白底是配深藍 `#1e3a63` 調的，
LINE 綠在深色模式下對比會掉。真要用官方版也該用黑色那張。

---

## 2026-09-10（三）　透析中心：交通加一張手繪 SVG 簡圖

作者問「你可以幫我畫或者給我 prompt」——**這裡不該用生圖**。地圖上全是
路名與站名，而長文大圖那份 prompt 檔開頭就寫了「中文字在生圖工具幾乎
必定變成亂碼」。SVG 的字是真的字：螢幕閱讀器讀得到、任何解析度都銳利、
路線改號只要改一行（這個月才剛改過兩條）。

畫的是示意圖不是比例尺，只留兩個站牌、兩個停車場、三條路，
外加一條虛線標「從路口下車後往哪走」。海安路、府前路拿掉——
對「怎麼走過來」沒幫助，畫上去只會讓路名互相打架。

### 窄螢幕的字級問題

SVG 等比縮放時字會一起縮：600 寬的 viewBox 到 393px 螢幕只剩 0.58 倍，
15px 的路名變成不到 9px。

第一版的解法是橫向捲動 + `min-width:480px`，但實測發現**最重要的
「西門、民權路口站」被切到畫面外**——要看公車資訊的人反而看不到。
改成把 `font-size` 從 SVG 屬性搬到 CSS class，窄螢幕用 media query 放大，
整張圖照樣縮得下。320 到 1280 五個寬度都完整顯示、不需橫捲。

### 兩個過程中的錯

**批次改 SVG 座標的腳本在第 6 項找不到目標時 `sys.exit`，前 5 項的
修改也一起沒寫入**——因為寫檔在最後一行。結果重跑檢查數字完全沒變，
一度以為是快取。改用逐項編輯。

**檢查腳本誤報「北」超出畫布**：它用 `getBBox()`，回的是群組內座標，
而指北針包在 `translate(48,58)` 裡，本地 x 是 −7～7，看起來就像跑到
畫布左邊外面。改用 `getBoundingClientRect()` 換算成相對 SVG 的座標。
**檢查失敗時先懷疑檢查**——這次又是一例。

---

## 2026-09-10（二）　透析中心：公車路線逐條查證，官網那張圖已過期

作者發現 5 號已改成 105，要求查其他路線有沒有一起變。查完的結果是
**官網那張交通圖比想像中舊得多**：

| 官網圖 | 現況 | 站牌 |
|---|---|---|
| 2 | **改號 102** | 郭綜合醫院（門口）✓ |
| 5 | **改號 105**（2026-08-21 都會公車升級） | 西門、民權路口 ✓ |
| 7 / 14 / 18 | 編號不變 | 西門、民權路口 ✓ |
| 綠17 / 藍23 / 藍24 | 不變（G17 / B23 / B24） | 西門、民權路口 ✓ |
| 239 | 高雄客運跨市路線，不在台南市路線表，查不到 | **移除** |

**另外官網寫「從火車站搭 14」已經不對**——現在 14 的起站是台南二中，
不是火車站。而 102 本身就經過台南火車站、又停醫院門口，是更好的答案，
改寫成「從台南火車站過來也是搭 102，一路坐到門口不必轉車也不用走」。

### 查證方式

`WebFetch` 讀不到（那一頁的路線全在圖片裡），`urllib` 直接抓 yunbus 會被
403 擋掉。最後是用瀏覽器逐條開路線頁、抓站名清單比對。yunbus 的路線 id
對得上台南市府 busmap 的 pathId（例如 TNN10164 ↔ pathId=10164），
站名用的是官方英譯，可以當交叉查證。

**239 選擇不列而不是照抄。** 查不到就不寫——這一段是給要來洗腎的人看的，
列一條無法確認的路線，比少列一條有害。

順帶加了兩則時效提醒：舊路線圖上的 2、5 就是現在的 102、105；
台南火車站的公車站位自 2026 年 3 月起因施工移到成功路與中山路。
文末的來源改成「接駁車與停車來自院方官網，公車路線 2026 年 9 月核對過」，
並補上大台南公車即時動態的連結。

---

## 2026-09-10　透析中心：交通與停車填完，機構事實 25/25

最後兩個 TODO。作者指定簡化郭綜合官網的交通資訊頁：
<https://www.kgh.com.tw/Introduce/Item07>

**那一頁的內容幾乎都在圖片裡**（`map.jpg` 與 `shuttle.jpg`），
文字版只有「機車計次、汽車計時」一句。WebFetch 讀不到路線，
所以改用瀏覽器開圖、下載後用 PIL 裁切放大，才讀得出九條路線與接駁車時刻。

### 怎麼簡化的

官網把九條路線的**起站、停靠站、迄站**全列出來。但要來看診的人只在意
「哪一班到得了、下車要走多遠」，起訖站是多餘的。所以拆成兩類：

* **2 號直接停「郭綜合醫院」站** —— 唯一到門口的
* 其餘八條停「西門、民權路口」或「西門路二段」，下車再走一小段

再補一句「從台南火車站出發搭 14 號」（火車站是它的起站），
這是唯一保留的起訖站資訊，因為它真的有用。

### 接駁車獨立成一節

郭綜合⇄成大醫院的接駁車，**郭綜合這側的上車點是「民生路 B 區大樓門口前」
——正好就是透析中心所在的 B 棟 3 樓那一棟**。對透析病人特別有用，
所以沒有揉進 transit 那一句，另外開了 `shuttle` 與 `shuttle_stop` 兩個欄位。
時間 09:10／12:10／14:10／17:10，週六日與國定假日停駛。

版面也從原本兩條 `<li>` 改成三個 h3（公車／接駁車／停車），
並在文末標示來源與「班次可能調整，出發前再確認」。

**機構事實 25/25，build 現在提示「資料填完了，要公開請把 PUBLISH 改成 True」。
沒有自己改——那是把整站對搜尋引擎開放，屬於作者的決定。**

---

## 2026-09-09（晚）　透析中心：營養訊息兩邊對齊成「量夠但挑品質」

作者把〈我們怎麼照顧一位透析病人〉的營養那句改成「吃得好而精，
而不是吃得多」。**但站上的衛教區 `col-diet` 寫的是反方向**——
「門診裡真正常見的問題，其實是吃得不夠」「需要的蛋白質比一般人多」。
同一個網站一邊說別吃太多、一邊說吃得不夠，讀者會無所適從。

回報之後作者確認真正的意思是**「量夠但要挑品質」**，所以兩邊一起調：

* 關於中心：補上「量要夠」這個前提，保留作者的用字
* `col-diet` 開頭：從「問題是吃得不夠」改成「該有的量沒到，
  而吃進去的東西又不夠好」
* `col-diet` 小標：「蛋白質要吃夠」→「蛋白質：量要夠，但要挑」，
  並補一段高生物價蛋白質 vs 加工肉品（後者蛋白質不見得多，磷鈉卻高），
  收在「量要夠，但要挑品質，不是靠份量堆上去」

改一句話之前先搜過全站有沒有相反的說法——這次搜到了。
單看那一句沒有錯，錯的是它和另一頁擺在一起。

---

## 2026-09-09（晚）　透析中心：關於中心頁開頭的留白

作者反映「我們是誰」上面留白太多。量出來：about.html 140px，
其他三頁都是 96px（手機 100 vs 56）。多的 44px 就是 `<h2>` 自己的
`margin-top` —— `section` 的 `padding-top` 已經是「和上一區的距離」，
h2 的 44px 是「和上一段內文的距離」，兩個疊在一起就多一截。
about.html 剛好是唯一開頭就放 h2 的頁。

修法是加一條 `.prose>:first-child{margin-top:0}`，順便把 education
與 services 裡各補一次的 `style="margin-top:0"` 兩處行內樣式移除
——以後新加的區塊不必記得補。

**驗證**：把改動前的產出用 `git archive HEAD dialysis` 取到
`C:\tmp\dbase`，兩份各起一個 server，逐一比對四頁裡每個
h2/h3/h4/p/ul 的 computed `margin-top`。**全站只有 1 處改變**
（about.html 的「我們是誰」44px→0px），其餘三頁 0 處。

這種「全域選擇器」的改動最怕的就是掃到別的地方，
而掃到了通常也不會噴錯——只能靠改動前後逐項比對。

---

## 2026-09-09（晚）　透析中心：合併〈照護內容〉與〈我們怎麼照顧一位透析病人〉

作者指定。這兩段本來就在講同一件事，而且有三組直接重複：
定期抽血／定期追蹤、通路追蹤／專責瘻管照護團隊、營養評估／飲食衛教。
〈照護內容〉原本掛在 `<h2>我們是誰</h2>` 底下，位置也不對——
那個 h2 講的是「我們有什麼」（團隊、規模、機器、設備），
「怎麼照顧」是另一件事。

合併後只留後面那個 h2，底下依時間軸分成五個 h3：
每一次透析／定期追蹤／血管通路／營養與衛教／需要的時候，
再接原有的〈初次透析〉。

**驗證方式**：把兩段裡出現過的 27 項具體事實（BioHole、無 Reuse、
副甲狀腺素、血庫、身心障礙證明…）列成清單，拿 git 取出的改動前 HTML
與重建後的 HTML 逐項比對，確認一項都沒掉。合併最容易出的錯就是
「讀起來順了，但少了一條」，靠眼睛看不出來。

第一次跑比對時忘了先重建，比到的是舊產出，兩邊當然一樣——
**產出檔的比對一定要先跑產生器**。

---

## 2026-09-09（晚）　透析中心：新增「初次透析」衛教

作者指定新增，內容參考國內各大醫院公開的「初次血液透析病人須知」類資料
（台大新竹分院、台中榮總護理部、北榮護理部，以及幾家透析診所的流程說明）。

`COLUMNS` 新增 `col-first`「第一次洗腎那天會發生什麼事」，**排在第一個**：
會點進衛教區的人，很多是剛被告知要開始透析、或家人剛開始，
他們最想知道的是「第一天會發生什麼事」。

`about.html` 的〈我們怎麼照顧一位透析病人〉底下加一個 `<h3>初次透析</h3>`
小節做簡短介紹，末尾連到完整那篇。

### 寫的時候的取捨

**各院不一致的細節一律不寫死。** 止血要壓幾分鐘、紗布留幾小時，
各家常規不同（查到的資料就有 15 分鐘、4–6 小時等不同說法），
寫成「依透析室當場的指示」——那是護理常規，要由中心自己說，
不是我從別家醫院的衛教單抄一個數字過來替本中心決定。

**不和站上既有內容打架。** 查到的資料多寫「兩次之間體重不超過 2–3 公斤」，
但站上 `col-fluid` 已經寫「乾體重的 3–5%」。同一個網站給兩個不同標準
會讓人無所適從，所以這篇不重述數字，直接連過去那一篇。

**楊桃寫成沒有轉圜餘地的禁令。** 其他飲食建議都有程度問題，
楊桃對透析病人是神經毒性，沒有「吃一點應該還好」——語氣上要區分開來。

**「第一次不會洗滿四小時」寫成普遍做法，不是本中心的承諾。**
這是標準的透析起始做法（避免不平衡症候群），但實際安排仍是醫師的決定。

圖 `col-first` 還沒放（12/13），缺圖時卡片顯示漸層佔位塊，不會破圖。

---

## 2026-09-09

### 透析中心：svc-hd／svc-access／svc-care 換上重生的圖

prompt 的 03、05、06。原圖只有幾百 px，壓出來 13–18 KB，擺在早就清楚的
svc-hdf（85 KB）旁邊一眼就看得出糊。新原圖 1448×1086 與 svc-hdf 同規格，
壓出來 69–72 KB，四張清晰度 28.8–34.6 落在同一個量級。

**這次不需要 `bump_assets.py`**：`sw.js` 第 50 行明確 `return` 掉
`/dialysis/`，透析站自己也沒有 service worker。跑 bump 顯示「未變動」
是正確的，不是漏跑。

還沒重生：`about-center`（578×462，差最多）、`band-process`（高度只有
221）、`hero`（1672×941，差一點點）。

### 透析中心：機台數欄位改成機型介紹

作者指定不放機台數，改放機型。`FACTS["machines"]` 這個 TODO 移除，
換成 `machine`（日本 NIPRO NCU-18）與 `machine_s`（NCU-18，給首頁
那排數據方塊用的短版）。待填欄位 3 → 2，只剩交通與停車。

規格是從 hcmed.com.tw（華江醫療儀器）的產品頁讀的，但站上掛的是**原廠
日本 NIPRO**——華江是台灣代理商，病人看到的機器上印的是 NIPRO。
第一版誤寫成華江，作者當天指正。

`about.html` 新增〈透析機〉一節，把製造商公開規格挑出跟治療有關的八項
（HDF、透析中 Kt/V、自動預充與返血、處方程式化、熱消毒、內毒素過濾器、
中央供液、IC 卡與網路）。

**當天再改一次**（作者提供原廠文案）：拿掉「可加裝內毒素過濾器」與
「可接中央供液系統」兩項，改寫加入自動預充／返血的細節（自動拍打人工
腎臟、腔室液面、專屬返血套件可不拔針）、重碳酸粉罐、透析液過濾器的
洩漏檢測與預充組件。原廠文案的「增加病患透析的安全性」沒有照抄，改寫成
機制描述（「少了一道在開放環境下調配的步驟，也就少了一個受污染的機會」）
——講機制不講療效，醫療機構的網站分寸在這裡。

同時預留機器照片版位 `machine-ncu18`：

* `make_dialysis_img.py` 新增 `PHOTOS` 這一類，**等比縮進方框、不裁切**。
  上面 `SIZES` 那批全是置中裁切，因為那些是照 prompt 生的圖、構圖可控；
  機器照不知道是直式還是橫式，裁固定比例會把機器的頭或腳切掉。
* `.mfig` 刻意**不用 float 讓文字繞圖**：下面緊接著一串 `<ul>`，清單項目
  符號碰到浮動元素的表現各家瀏覽器不一致。多花垂直空間換版面穩定。
* 用 900×1200 與 1200×675 兩張假圖實測過：直式高度收在 460，橫式縮到
  237／195，兩種都不溢出。**假圖測完已刪**——`dialysis/img/` 是產出目錄，
  留在那裡會被 commit 上線。
* 檔案沒放就整塊不出現（不留破圖也不留佔位框），並列進 build 的圖片盤點
  （`11/12 已放（缺 machine-ncu18）`）。不列進去的話它會安靜地不出現，
  而頁面看起來完全正常。

⚠ **這張照片不能用 AI 生圖**。其他圖是示意的空間與情境，畫錯頂多氣氛不對；
這張要拍的是一台實際存在、型號就寫在旁邊的機器，畫出來的東西掛上去就是
錯誤資訊。要嘛拍中心裡的實機，要嘛用原廠授權的產品照。

### 拿到原廠目錄後重寫（同日稍晚）

作者提供 `NCU-18-A3雙面中文目錄6-FINAL.pdf`（雲端硬碟）並授權取用照片。
機器照從 PDF 內嵌圖抽出（`fitz`，xref 92，1513×4547，1:3.01 —— 和機身
480×1470mm 的比例吻合），存成 `img_src/machine-ncu18.png`。
白邊試著裁過但量出來四邊都有內容，本來就是滿版，不必裁。

⚠ **PDF 目錄最下面那行 `＊為選配零件` 是這次最重要的發現。** 標星號的有：
HDF 前／後稀釋、中央供液系統、重碳酸粉罐 BICART、內毒素過濾器 CF-609N、
護理輔助作業（自動預充／返血／排液）、NIBP。也就是說**站上寫的 HDF、
BICART、自動預充與返血，在原廠規格裡全是選配**。這三項留著是因為：
中心本來就把 HDF 當服務項目在推，另外兩項的文案是作者自己給的。
但這是需要院方確認的事，不是可以從目錄推出來的。

被作者先後指定移除的中央供液與內毒素過濾器，剛好也都是選配 —— 
作者顯然在照實際配備篩選。

版面改成左圖右文（`.mrow` grid）。照片 1:3 比文字高很多，第一版右欄下面
空一大塊，把「治療上做得到什麼」整份清單也移進右欄才平衡。
≤560px 收成單欄並把圖限寬 170px —— 1:3 的圖佔滿寬度會高得離譜。

新增的內容都是**非選配**的標準功能：停電時撐 30 分鐘以上（血液幫浦、
肝素泵、動靜脈壓、氣泡偵測）、脫水誤差 ±30 g/h、氣泡超音波偵測
（單顆 ≥10μL）、漏血光學偵測、透析前自我檢測、緊急處置按鈕、四色警示燈、
10.4 吋繁中觸控螢幕、機身寬 30cm，以及**衛部醫器輸字第 025316 號**
（可查證的許可證字號，比任何形容詞都有用）。

⚠ **兩件事刻意沒做**：

1. 沒抄製造商的宣傳語（「最佳的醫療品質」）。醫療機構的網站不適合用
   最高級形容詞，只寫機器實際具備的功能。
2. 型號來自作者提供的製造商產品頁，**不是院方公布的設備清單**。
   中心實際是否全數使用這一型，仍要由院方確認——註解裡寫清楚了。

---

## 2026-09-08

### 遊戲區三頁：放大「回首頁」品牌，順手修掉寫死的 sticky 位置

`shop.html`／`game.html`／`library.html` 的回首頁連結原本沒有自己的字級，
直接繼承誠實橫幅的 13px，和旁邊的說明文一樣大，看起來不像一個入口。
shop 與 game 放大到 16px，並加 640／430／365 三段縮字。

**library 刻意不放大到 16px。** 它的文案「收藏冊 — 未取得的格子會留空，
一眼看得出還差哪幾張」有 26 字，是三頁裡最長的，實測放大會把 480px
也擠成兩行。改成 13.5px，縮字門檻從 365 提早到 390（375px 在 12px
那一級差 12px 才放得下）。結果仍比改動前好：改動前 430px 以下全兩行，
現在只剩 320px 兩行（差 36px，要補只能縮到 9px 或藏起品牌文字，不值得）。

⚠ **三頁的 `header` 原本寫死 `top:36px`**，那是單行橫幅的高度。橫幅一
換行就對不上——捲動後桌機差 2px、library 在 393px 差 23px（頁首壓在
橫幅上）。改成 `top:var(--hnh)`，由一小段腳本量橫幅實際高度寫回。

**這個坑很難發現**：不捲動時 header 本來就緊貼橫幅，`top` 寫多少都一樣，
在 scroll 0 量「縫隙」永遠是 0。要捲下去（測試用 600px）才量得到。

### 每日文獻摘要：一句 Telegram 就能上線

新增 `publish_digest.py`。作者在 Dr. Shock BOT 的對話裡回一句
「2026/9/7 可上線」，bot 就跑完匯入→建站→bump→check→commit→push。
審核權還在作者手上（三大期刊來源可信，但「值不值得放上民眾看的網站」
是人的判斷），省掉的只是審完之後那串沒必要人打的指令。

`import_digests.py` 加 `--only YYYY-MM-DD`（可重複）：審過哪一天就只上
哪一天。指定的日期沒有可解析的摘要檔會直接中止——默默當成「那天沒東西」
會讓人以為已經上線了。

`publish_digest.py` 刻意保守，任一條不過就不動作：

* 工作區有沒 commit 的改動 → 中止（免得把改到一半的東西一起送上線）
* 本地落後遠端 → 中止
* `check_site.py` 沒過 → 不 commit
* `git add` 逐一列路徑，不用 `-A`（理由同第一條）

驗證方式：把正式庫 clone 成「自己的 bare 遠端＋工作區」，在裡面把一篇
退回沒上線的狀態再跑一次，確認動到的 8 個檔案與手動跑的完全相同，
產出 hash 也一致，而且真的 push 到那個遠端——正式站完全沒被碰到。

第一次測的時候只退回 `news.json` 沒退回產出頁，結果 commit 只有 1 個
檔案。那不是 bug，是測試沒設好；**產出頁也要一起退回**才測得到重建。

Bot 端的觸發詞是句尾的「上線」，另外擋掉「不要／別／先不／暫緩／取消」
等否定句。誤觸的代價是把東西推上正式站，所以寧可漏接。

### 醫學新知 27 → 30 篇

匯入 9/7、9/8 摘要的三篇：Nefecon NefIgArd（Lancet 2023）、
AURORA 1 voclosporin（Lancet 2021）、FIDELIO-DKD（NEJM 2020）。
`date` 是原始發表日不是匯入日，所以它們不會排到最上面冒充新研究。

---

## 2026-09-07

### 首頁：移除「這個網站怎麼用」、區塊間距收緊

六格導覽磚拿掉，但**沒有刪掉原始碼**——`HOWTO = False` 開關，改成 `True`
重跑就完整回來（和 `DIALYSIS_LINK` 同一套做法）。版面、文案、六個連結都
還在 `build_home()` 的 `howto` 變數裡。那六個入口沒有消失：三個在頁首
「衛教文章▾」，三個是頁首獨立的 tab，首頁往下捲也都還有各自的區塊。

`.sect` 上方留白 44px → 26px，`.hero + .sect` 14px。標題自帶左邊色條，
分界靠那條線就看得出來；44px 在手機上等於每捲一區就少半屏。
首頁高度（1280px）5,216 → 4,867。

### 頁首標籤中間多一個 4px 空隙

`.ngbtn` 是 `display:inline-flex` 加 `gap:4px` 來推下拉箭頭，但按鈕裡是
`<span class="np">衛教</span>文章`——「文章」是匿名文字節點，**在 flex 裡
自成一個項目**，gap 就插進標籤中間，渲染成「衛教 文章」「新知& 指引」。
間距改掛在 `.ngcaret` 的 `margin-left`。從加下拉選單起就一直在，各寬度都有。

### 首頁醫學新知卡改連站內完整摘要

標題原本連 `doi.org` 且 `target="_blank"`，按下去跳出站外看到付費牆。
改連 `/articles/news-<分類>.html#<錨點>`。整張卡可點：用標題連結的
`::after` 撐滿整張卡（stretched link），**不把 `<article>` 包進 `<a>`**——
巢狀連結在 HTML 裡無效，而且卡片裡的文字還要能選取。

### 醫學新知：24 → 27 篇，並改成「每日三篇一律上網站」

新增 PROTECT（Lancet 2023）、STARRT-AKI（NEJM 2020）、CONVINCE（NEJM 2023）。

**`pub_date()` 的坑**：只認得「2026年9月2日」和「2026-09-02」兩種寫法，
期刊引用格式 `N Engl J Med 2020;383:240-251` 解析不出來就**回退成
「摘要產生日」**——2020 年的 STARRT-AKI 被標成 2026-09-07 排到最上面
變成「最新研究」，首頁那張卡也會選到它。改成再抓一次出處行裡的
**第一個**四位數年份（不是最大的：頁碼可能長得像年份，`2015;372:2010-2020`）。

⚠ 修完之後我一度用新函式把全部 27 筆重算，把 19 筆手工標的 `YYYY-MM`
降級成只有年份。從 git 還原後只重跑匯入才對。**只有壞掉的那幾筆需要重算。**

作者 2026-09-07 定調：**不再逐篇挑，每天三篇直接全部匯入**。所以
`import_digests.py` 的「這篇沒收進來」變成需要被看見的事——原本
「期刊不收」與「欄位不全」都回 `None`，混成同一堆看起來像解析失敗；
改成回傳原因字串並分組列出，且 `NEW_RULES_FROM = "2026-09-06"` 之後的
檔案只要漏收就單獨跳警示。

`daily_covered.json`（每日摘要的排除清單）也清成只留網站上有的 27 筆，
細節寫在 `nephrology_digest\CHANGELOG.md`。ALIGN 那篇原本只有 Lancet
文章網址、沒有 `doi`，排除清單照 DOI 比對就排不掉它，用 CrossRef 查證後補上。

### 透析中心：圖片到齊

作者把原圖放進 `dialysis/img/`（**輸出目錄**），四張 `col-*` 是 2816×1536、
每張 2–3 MB 就這樣被網頁載入；`hero.png` 放那裡則完全不會被讀到，
程式找的是 `hero.jpg`。移到 `img_src/` 重跑 `make_dialysis_img.py`：
13.6 MB → 0.6 MB。圖片 11/11。

頁首標誌 26px → 34px：生成的標誌四周有 **27% 透明留白**，26px 時筆畫實高
只剩 19px。換一個沒有留白的標誌檔時記得改回 26。

### 旅遊透析長文刪除「那工作呢？」一節

文章開頭的重點問答也一併拿掉工作那半句——段落刪了問答留著，
等於承諾一個文章不再回答的問題。目錄由標題自動產生，不必另外改。

---

## 2026-09-05

### 「衛教文章」也做成下拉；「依主題閱讀」改名「主題衛教」

頁首兩個下拉：**衛教文章**（深入文章／主題衛教／衛教圖卡）與
**新知＆指引**（新知／指引）。

`navgroup()` 加 `gid` 參數——第一版把面板的 id 寫死成 `ngm`，
兩組同時存在就會有兩個相同 id，`aria-controls` 全部指向第一個。
腳本也從「只認一組」改成走訪全部，一次只開一個。

**新增 `articles/topics.html`**：三個子項各自需要一個落點，
所以把原本只是 `/articles/` 上一個區塊的「主題衛教」獨立成頁。
`/articles/` 仍是總覽，從頁尾與首頁進得去。

**頁首標示要分得夠細**：八個分類頁（`blood-pressure.html` 之類）歸
「主題衛教」，長文歸「深入文章」——讀者從哪一支點進來，回頭就要指向同一支。
第一版全部歸到「深入文章」，分類頁的標示是錯的。

#### 斷點第四次重算：兩個下拉箭頭各佔約 15px

`320–328`、`366–390`、`411–450` 三段換行，每一段都卡在該階的上緣。
門檻整批往上推：中間階 410 → **450**，最緊階 365 → **390**；
`≤330` 再加一條把箭頭收掉（省 30px）——那個寬度下排成兩排的代價，
比少一個「這裡會展開」的提示大。

**掃描方式也修了一個長期的錯**：原本用 y 座標判斷是否同一排，
但按鈕與連結高度不同、y 本來就不一樣，會一直誤報。改成看垂直重疊。
（這個坑在字級／配色那一排踩過一次，這次又踩了。）
149 個寬度重掃：全部一排、零溢出。

### 新知改用結構化摘要格式，研究排到指引之前，首頁加新知區

作者指定「仿照 kidney digest 的每日任務格式」。查了才知道那是**作者自己另一個
session 的產物**——`C:\Users\user\nephrology_digest\`，每天由排程產生一份
「腎臟病學每日精選摘要」。格式是 JAMA 式結構化摘要：

> 期刊標籤／中文標題／英文原標題／出處行／
> **KEY POINTS（問題 Question · 發現 Findings · 意義 Meaning）**／
> 研究背景 BACKGROUND／研究方法 METHODS／主要結果 RESULTS／臨床意義／限制

`digest_card()` 照這個結構做，但**配色不照抄**——那份用暗紅，這個站是青綠，
兩邊的視覺要能分得出來。

**欄位可以留空**：查不到的段落就不渲染，不為了版面完整去編。
例如 sibeprenlimab 那篇只查得到 Key Points 與主要結果，
背景與方法兩段直接不出現。

新增三篇（研究共 5 篇）：
- **豬腎異種移植橋接同種移植**（Lancet 2026-09-03）——直接取自作者那份
  每日摘要，比我自己搜到的 NEJM 2025 那篇更新。
- **Sibeprenlimab 治療 IgA 腎病變**（NEJM 2026，第三期期中分析）
- **Atrasentan / ALIGN 2.5 年最終結果**（Lancet 2026）

版面順序也照指示改：**研究在上、指引在下**。首頁在「深入文章」之前加
「新知」區，只放最新一篇的精簡卡（到 Key Points 為止），
右下角「閱讀更多」進新知頁——長文隨時可讀，新知會過期，會過期的要排前面。

### 新增分頁「新知＆指引」（`articles/updates.html`）｜頁首回到六項

作者指定。收現行的國際指引與真正改變處置的研究，取代原本散在各長文
「參考來源」裡的零星引用——那些只證明單篇文章的依據，湊不成一份總覽。

**每一則兩層**：先一句白話（給病人與家屬），再一段專業重點與正式出處
（給同業，也是給搜尋引擎的 E-E-A-T 訊號）。分兩層而不是各做一頁，
是因為同一件事拆兩頁會互相搶關鍵字。

目前 10 份指引（KDIGO 4、KDOQI 2、ADA 1、AHA／ACC 1、ESC 2）
＋ 2 篇研究（FIND-CKD、FLOW）。

#### 每一則都查證過，沒有一則是憑印象寫的

年份、期刊、卷期逐一查過，過程中修正了三個原本會寫錯的地方：

- **KDIGO 貧血指引是 2026 版不是 2012 版**——2026 年 1 月才發表，隔了十四年。
- **KDIGO 糖尿病與 CKD 現行仍是 2022 版**。2026 更新版的公開審查 4 月才結束，
  尚未正式發表，頁面上明講這件事，不能寫成「2026 版」。
- **AHA／ACC 高血壓是 2025 版**，2017 年以來第一次大改版，
  風險計算改用 PREVENT 取代 Pooled Cohort Equation。

`UPDATES_REVIEWED` 是手動維護的盤點日期，**刻意不用建置日自動帶入**——
理由和透析中心的 `NOTICE_CHECKED` 一樣：那會變成每次建置都宣稱今天盤點過。

也刻意不做 PubMed／RSS 自動抓取。那要排程與金鑰，而指引改版是以年為單位
的事，手動維護足夠。

#### 頁首斷點：最緊那一階從 ≤365px 放寬到 ≤440px

第六項加回來之後，**375–430px 全部換行**——iPhone SE 375、13/14 的 390、
14/15 Pro 的 393、Pro Max 的 430，正好是最常見的一批。
而 360 與 344 反而排得下，因為它們已經落在最緊那一階。

把門檻拉到 440 讓所有手機都吃到最緊的一階，六項才排得下一行。
代價是項目間距歸零、字級 13px；垂直內距不動，點擊區維持 42px。
實測 320–1600px 共 14 個寬度全部一排。

**加第七項之前一定要重跑寬度掃描**——這已經是第三次因為項目數改變而
重算斷點了（2026-09-04 加常見問答、09-05 拿掉圖卡、09-05 加新知＆指引）。

#### 這個斷點我改錯了兩次，兩次的教訓不一樣

**第一次**：把最緊那一階從 ≤365 拉到 ≤440，讓所有手機都吃最緊的設定。
六項是排得下了，但那一階同時收 `.brand`，440px 時標誌被縮到 12.8px／74px，
中間卻空著 146px。作者反映「頁首比例有問題」。
教訓：**導覽排不下是導覽的問題，不要把標誌連坐**。

**第二次**：只把標誌拆回 ≤365，導覽仍留在 ≤440。作者再反映
「手機直式還是特別小且擁擠」——也對，因為**排得下不等於排得好**。
實測各組合的導覽實寬與最低可用寬度：

| 內距／字級／間距 | 導覽實寬 | 最低可用寬度 |
|---|---|---|
| 8 / 13.5 / 2 | 268px | 411px |
| 5 / 13.5 / 2 | 232px | 366px |
| 2 / 13 / 0 | 180px | 320px |

393px 時本來空著 90px、430px 時空著 119px，字卻縮到 13px、間距歸零。
**真正需要最緊設定的只有 ≤365px。**

**第三次（現行）**：回到三階 700／410／365，值不變，只是六項而非五項。
**標誌與導覽的門檻必須寫在同一個媒體查詢裡**——我一度把導覽切在 401、
標誌切在 410，402–410 這段標誌落回大的一階、多出 8px，剛好讓導覽換行。

驗證方式也升級了：不再抽樣幾個寬度，而是 **320–460px 逐 1 像素掃描
共 141 個寬度**，全部一排、零溢出、點擊區 42–43px。


### ⚠ 定位變更：本站可以放就醫資訊，也可以蒐集個資

作者指定，**取代先前所有「勿加診所／掛號資訊」「維持無招攬」的原則**。
這條寫在最前面，因為它會影響往後每一個「這樣寫會不會太像廣告」的判斷。

現在可以做的：宣傳就醫資訊與門診時段、用 LINE 或其他方式蒐集病患個人資料。

**還是不能做的**（醫療法 §85–87 對醫療廣告的內容規範沒有跟著鬆綁）：
療效保證、比較性用語（最好、第一、權威）、病人見證與治療前後對照。

**作者當天稍後澄清的模式**：網站本身不蒐集任何資訊——靜態頁、無表單、
無後端。LINE 與 email 只是留在頁面上、讓病患主動聯絡的管道。

所以**不需要隱私權政策頁**，也沒有個資法第 8 條的告知義務——放一個帳號和
一個信箱不構成「蒐集」。我一度主張需要，是把「網站」和「收件匣」混為一談：
真正拿到資料的是收件匣那一端，那和診所的電話一樣，屬於醫療機構本來就有的
病患資料框架，不是這個 repo 要解決的問題。

哪天改成「在網站上填表單送出」，上面那套才會重新適用。

**還沒對齊的是文案，不是法規**：`about.html`「聯絡方式」那段現在明寫
本信箱**不提供**個人醫療諮詢、不解讀報告、不回覆病情問題。要把 email／LINE
當成病患聯絡管道的話，那段要改成說明「每個管道各自受理什麼」。

### 關於作者頁的透析中心連結（`DIALYSIS_LINK`，目前關著）

作者要求「做好但先不上線」，所以做成 `build_site.py` 最上面的開關，
預設 `False`。改成 `True` 重跑再推就會出現，不必再改別的地方。

**為什麼用開關而不是先寫好再手動刪**：`/dialysis/` 的機構事實還沒填完、
整站是 `noindex` 狀態，從這裡連過去等於讓人提早找到半成品。
而寫好放著、關掉輸出，比記在腦子裡「以後要加」可靠。

關著時 `about.html` 與上一版**逐位元組相同**（第一版少寫一個字，
空的插槽留下一行空白，已修掉）。

### 透析中心站：`PUBLISH` 開關（作者要求「可以進去看，但不要有連結」）

現況本來就是作者要的：檔案在 GitHub Pages 上、知道網址就打得開，
但全站 39 頁沒有任何連結指過去，五頁 `noindex`，也沒有 sitemap。

補的是一個**還沒發生的問題**：原本 `noindex` 只綁 `ready()`（資料填完了），
所以把 `FACTS` 填完的那一刻，網站就自己對 Google 開放了——
而填資料的人多半只是想「先填一填看看長怎樣」。院方確認、對外時機，
那是人要決定的事，不是資料完整度能代表的。

拆成兩個判斷：`ready()` = 資料填完了，`live()` = `PUBLISH and ready()`。
`noindex` 與 sitemap 都改看 `live()`。`PUBLISH` 預設 `False`。

**這個開關管的只有搜尋引擎收不收錄**，不影響「知道網址就打得開」——
那正是作者要的狀態。三個狀態都測過：資料沒填完、填完但沒 PUBLISH、
兩者都成立，`noindex` 分別是 5/5、5/5、0/5。

### 透析中心站補齊四件事：sitemap、流量統計、公告、醫師陣容

**`ready()` 取代原本只看 `FACTS` 的判斷**，現在同時要求 `DOCTORS` 有內容。
它一次決定兩件事：頁面要不要輸出 `noindex`，以及要不要產生
`dialysis/sitemap.xml`。**綁在一起是刻意的**——擋著搜尋引擎卻又遞給它一份
網址清單，是自相矛盾的。還沒 ready 時會把既有的 sitemap 刪掉，不留誤導殘檔。

那份 sitemap 和主站的是兩份，上線時要各自在 Search Console 提交。
沒有把它加進主站的 `robots.txt`——兩個產生器互不知道對方的狀態，
指向一個可能不存在的檔案比不指更糟。

**Cloudflare Web Analytics 用和主站同一個 token**：CF 的統計按網域算，
兩個站都在 kidneygod.net 底下，同一個 token 就會在同一個儀表板分路徑呈現。
換 token 時 `build_site.py` 與 `build_dialysis.py` 兩個檔案都要改。

**停診公告從寫死改成 `NOTICES` 清單**。原本永遠寫著「目前無停診公告」——
一個沒人維護的公告欄比沒有公告欄更糟，讀者會相信它然後白跑一趟。
沒有公告時改為附上 `NOTICE_CHECKED` 最後確認日期。
**這個日期刻意不用 `TODAY` 自動帶入**：那會變成每次建置都宣稱「今天確認過」，
等於用日期說謊。

**`doctors` 從一個字串改成 `DOCTORS` 結構**（姓名／職稱／專長），
渲染成 `.docs` 卡片。多位醫師用頓號串成一段的話，
讀者要找「誰看什麼」得整段掃過去。

`line` 欄位保留，用途確定為**洗腎室服務專員**的聯絡窗口（作者指定），
標籤與待填提示都已對齊。

### 新站：郭綜合醫院血液透析中心（`/dialysis/`）｜`build_dialysis.py`

**和護腎教室完全分開的第二個站**，共用網域與 GitHub Pages 部署，
不共用任何程式碼、CSS 或設計語言。放同一個 repo 只是為了省下第二套部署。

版面語言參考 `wecareheart.com` 的組織方式（雙語小標＋大標、卡片區塊、
捲動淡入、深藍頁尾、右下角浮動預約鈕）。那個站是 WordPress ＋ Elementor 產的，
**只借「版面怎麼組織」，程式與文案全部自己寫**——他們的 CSS 與文字是人家的。

配色 `#1E3A63 / #2E7FB8 / #14807A`、標題 Noto Serif TC、內文 Noto Sans TC。

**五頁**：首頁、關於中心、服務項目、透析衛教、就醫資訊。

#### 機構事實集中在 `FACTS`，未填會擋索引

地址、電話、機台數、醫師陣容這一類**關於一間真實醫療機構的事實**，
全部集中在 `build_dialysis.py` 最上面的 `FACTS`，沒有散在各頁文案裡。
還沒確認的值包成 `TODO(...)`，頁面上渲染成黃底的「待填」標記。

**只要還有任何一個 `TODO`，每一頁都會自動輸出 `noindex,nofollow`**，
填完之後那行會自己消失。帶著「待填：透析室電話」被 Google 索引，
比晚一點上線糟糕得多，而這種事不能靠記得。

#### 捲動淡入刻意不用 IntersectionObserver

整個版面靠 `.reveal{opacity:0}` 起始，只要觀察器因為任何理由沒回呼，
**整頁就是全白**。實測在內嵌式預覽視窗裡它一次都沒觸發過。
改成自己量 `getBoundingClientRect` 加 rAF 節流，再加一道三秒保險
（`setTimeout(showAll, 3000)`）——動畫沒播只是可惜，內容看不到是災難。

#### sw.js 排除 `/dialysis/`

護腎教室的服務工作者對圖片是「快取優先且不看查詢字串」，
會讓這個站換過的圖永遠停在舊版。整個子目錄直接不接管。

#### 三個踩到的坑

- **`<div class="card"{rv(...)}>` 會產生兩個 `class` 屬性**，瀏覽器只認第一個。
  八張卡片的動畫因此全部失效，而且不會噴錯。`rv()` 改成把原有的 class
  一起收進去，寫法上就不可能再犯。
- **`.todo{white-space:nowrap}` 把版面撐爆**：待填的字比實際值長得多，
  320–1024px 全部產生橫向捲軸。偏偏是在還沒放真資料、最容易被忽略的階段。
- **`.reveal` 的起始 `translateX(30px)` 本身會造成溢出**（393px 手機多 6px）。
  `section{overflow-x:clip}` 當安全網，並且左右進場只在雙欄版面（≥861px）用。
  用 `clip` 不用 `hidden`：後者會建立捲動容器，把 `position:sticky` 弄壞。

#### `check_site.py` 第 5 項跟著改

原本只讀 HTML 找 `text-size-adjust`，但新站是連外部樣式表，會誤報五頁。
改成連本地的 `<link rel=stylesheet>` 一起讀進來。

### 頁首拿掉「衛教圖卡」，改從衛教文章頁進去（剩五項）

和 9/04 的「常見問答」同一個處理：圖卡本來就是衛教內容的另一種呈現，
和長文、分類並列在同一頁比自成一個分頁更貼近讀者的心智模型。作者提的。

**入口從一張薄卡片改成把五個系列攤開**（`.cats`，和上面的「依主題閱讀」
同一套視覺），右下角再一個「全部圖卡總覽 →」。原本只有一張寫著「107 張，
依主題分類」的卡片，點進去還要再選一次系列——而讀者是照系列在找的。
`build_index()` 因此多收一個 `series` 參數，`load_series()` 從
`build_gallery()` 裡抽出來共用。

**兩處回頭路**：圖卡總覽頁本來沒有任何返回連結（靠頁首那個分頁就好），
拿掉分頁之後會變成死路，補上 `← 衛教文章`；系列頁原本的「依主題閱讀 →」
也改指到 `/articles/#gallery`。

**頁首標示**：`in_articles` 原本排除圖卡頁（因為它自己有一項），現在改成
`path.startswith("articles/")`，圖卡頁會標在「衛教文章」上。

實測 320–1600px 共 15 個寬度，五項全部一排，**頁首高度從 65px 降到 59–61px**。
手機的字仍是縮寫版（文章／食物／計算／作者／遊戲）：量過完整字要 339px、
375px 手機只有 335px，差 4px，還是排不下；540px 以上才排得下。

### 首頁分享圖示併進頁首下方那一排

原本在「這個網站怎麼用」下方自成一列，移到 `.prefbar` 的左側——那一排本來
只有右邊的字級／配色控制項，左邊整片是空的。作者提的，理由就是那排有位置。

`FSBAR` 常數改成 `prefbar(left="")` 函式，`page()` 多一個 `pref_left` 參數。
靠 `.prefshare{margin-right:auto}` 而不是 `justify-content:space-between`：
其餘三十頁沒有左插槽，用 space-between 會讓它們的控制項跑到左邊去。

`.prefbar` 加 `flex-wrap:wrap`。四顆分享鈕（206px）＋兩顆控制鈕（96px）＋間距
＝ 310px，**360px 以上的手機排得下，344px 以下會自己掉成兩行**——不縮按鈕，
44px 的觸控下限比擠成一行重要。

**這一排在 `.zoomable` 外面**，所以不吃 `--fs` 的 zoom，也就不需要 `--fsi`
抵銷。原本那組在內容區裡，是靠 `.zoomable .share{zoom:var(--fsi)}` 抵銷的。

### 新增長文〈洗腎之後還能旅行嗎〉（第 14 篇）｜`37ac7a0`

作者原本問的是「把旅遊段落加進既有文章就好，還是需要一篇長文」。選長文的
理由：血液透析（要預約旅行透析、要核退）與腹膜透析（要配送透析液、要診斷書
過海關）兩條路徑的準備完全不同，塞進〈三種腎臟替代療法〉會把那篇的主線
（怎麼選）稀釋掉；而且「洗腎還能不能出國」本身就是一個會被搜尋的問句。

內容取自作者指定的兩份來源（有元診所衛教問答、腹膜透析腎友協會），加上
`PAGE_SOURCES` 的 `capd`／`nhi`／`tsn`。三家航空公司（長榮／星宇／國泰）的
醫療需求頁**是逐一連線確認過 200 才寫進去的**（`scratchpad/airlines.py`），
因為航空公司的網址改版很勤，寫死連結是會爛掉的那種內容。

同步改動：〈三種腎臟替代療法〉兩處加延伸閱讀；FAQ 那題「洗腎之後還能不能
工作、出國？」改指到本篇，另加兩題 → 55 題。

**還缺一張大圖**：`hero/dialysis-travel.jpg` 還沒有，所以首頁與長文列表顯示
`.mph` 漸層佔位塊，OG 圖退回標誌。

### 「閱讀更多」與兩個完整清單頁｜長文 14 篇、FAQ 55 題

首頁只留 10 篇長文（`HOME_FEAT_MAX`）、文章索引 16 篇（`INDEX_FEAT_MAX`）、
索引頁的常見問題只留 6 題（`INDEX_FAQ_MAX`），其餘收進兩個新頁：
`articles/long-form.html`、`articles/faq.html`，右下角用 `.morelink` 進去。

**沒有為它們加分頁標籤**，這是作者明確指定的——手機頁首已經很擠（見下一則），
再加就要換行。入口只從內容區進去。

### 頁內主題切換與字級控制（手機縮成兩顆圖示）

`FS_BOOT` 放在 `<head>` 先寫入 `--fs`／`--fsi`／`data-theme`，避免閃一下。
字級用 `zoom:var(--fs)` 掛在 `.zoomable`——全站 CSS 都是 px，改 root font-size
沒有用。分享按鈕用 `--fsi`（倒數）把 zoom 抵銷回來，否則放大時按鈕會變巨大。

手機版兩顆圖示（`.preficons`）、桌機維持原本的文字列（`.preffull`），兩套 UI
共用同一份狀態、互相同步。

**踩到兩個坑**（都已列進「已知的坑」）：`svg.hidden = false` 對 SVG 元素無效；
`:root[data-theme="dark"] a{}` 的權重會壓過 `.sb{}`，害深色模式分享按鈕變藍綠色。

### JAMA 式「重點」方塊（13 篇長文全上）

作者問「整站改 JAMA 風格如何」——**我建議不要**：JAMA 那套是為了在密集的
論文版面裡快速掃描，這裡的讀者是病人，密集排版反而更難讀。只借一樣東西：
Key Points 方塊。

`parse_article()` 讀三行 `重點問題／重點答案／重點行動`（順序不拘，三行都有
才渲染），`keypoints_html()` 用 `<dl>` 輸出——標籤與內容成對，讀螢幕軟體
念起來才對。

### 版面隨螢幕寬度放寬（頁首、內文、頁尾一起）

作者反映寬螢幕上網站顯得窄。`WIDE_PAGES` 的五頁改用較寬的 `--maxw`，而且
**頁首與頁尾的 `.wrap` 也要一起換**，否則導覽列會和內容切齊不了。

格線一律 `minmax(min(220px,100%),1fr)`：`minmax(220px,1fr)` 在特大字級下
會撐爆容器，`1fr` 的最小值是 `auto`、圖片縮不下去。

---

## 2026-09-04

### 新增「常見問答」分頁（14 題）｜`97991db`

`build_site.py` 的 `FAQ_GROUPS` + `build_faq()` → `faq.html`。

**為什麼要獨立一頁**：站上的分類是「血壓管理」「血脂代謝」這種醫學分類，
但病人腦子裡是一個問句——「報告紅字是不是腎壞了」。這一頁把入口從
「網站有什麼」翻成「你想問什麼」。參考 wecareheart.com 首頁把 FAQ 攤開的做法，
但作者指定**做成獨立分頁、不動現有首頁結構**。

14 題分四組（看懂報告／日常照顧／用藥與保健食品／洗腎與治療），
每題 80–150 字能單獨成立的答案，再接到對應長文——**13 篇長文全部被串到**。
FAQPage 結構化資料、sitemap、站內搜尋都已納入。

`.qa h3` 覆寫全站的灰色 `h3`：長文裡 h3 是次級標題，這一頁**問句才是主角**。

### 頁首七個項目的斷點重算（四處）

加入「常見問答」後從六個變七個，四個地方全部重量過。
**最反直覺的一點：桌機也會爆。**

`.wrap` 的 `max-width` 是 `--maxw`（720px），所以**頁首可用寬度永遠只有
680px，和螢幕多寬無關**——實測 960px 視窗一樣是兩排。

| 位置 | 原本 | 改成 | 原因 |
|---|---|---|---|
| 桌機 `nav a` 水平內距 | 11px | 9px | 七個四字標籤正好卡在 680px |
| 第一個斷點 | 700px | 719px | 701–719 時 `.wrap` 還沒撐到 720 |
| 第二個斷點 | 380px | 430px | 381–430 餘裕只剩 8px |
| 第三個斷點 | 340px | 365px | 341–365 會換行 |

用 Playwright 掃 24 個寬度（320–1400）驗證全部一排、頁首高 57–61px。
**下次再加項目一定要重掃一次**，四個門檻是連動的，改一個會把問題推到隔壁。

### 長文大圖：首頁雜誌式陳列、文章頁首圖、專屬預覽圖

`hero_src/`（原圖，不進版控）→ `make_hero.py` → `hero/<slug>.jpg`（1600×900）。

- **首頁「深入文章」改成雜誌式卡片**，最新一篇佔滿整排當封面。
  排序從檔名字母序改成發布日新的在前——雜誌用字母序沒有意義。
- **文章頁標題之後、導言之前**放同一張圖。`alt=""` + `aria-hidden`：
  圖沒有資訊量，標題與導言已經說完了。
- **`make_og.py` 分兩種來源**：13 篇長文用自己的大圖裁 1200×630，
  其餘 23 頁沿用 logo。裁切來源取 `hero_src/` 的原圖而不是壓過的 1600×900。

原圖 13 張共 26.6 MB，`make_hero.py` 壓到 **1.6 MB（6%）**。
1600 寬的依據：內容欄 680px，封面滿版時 1600 超過 2 倍視網膜所需的 1360。
**`--maxw` 若調寬要回頭檢討這個數字。**

### 首頁與衛教文章總覽：深入文章移到依主題閱讀之前

兩頁順序一致。衛教文章總覽的分類卡片原本沒有標題（緊接導言下面讀得通），
被長文區隔開之後補上「依主題閱讀」，用詞與首頁相同。

該頁的長文**仍照檔名排序**，不跟首頁一樣「最新在前」：總覽頁給人回頭查找，
穩定的順序比較有用；首頁才是凸顯新內容的地方。

### 關於頁資歷改為分組陳列

原本十項平鋪，改成現職／學歷與經歷／專科執照與認證／學會與委員會四組。
**內容完全沿用原本那十項，沒有新增任何宣稱**；作者指定不加「受邀演講」。

### 修跑馬燈：第一次進站空白、跑到一半跳掉｜`b4edf18`

兩個症狀同一個原因——動畫先開始、內容後到。

- **空白**：字原本由頁尾腳本填，而頁尾要等 `shared.js` + `data.js`，
  實測 690ms。把文案與第一次填字移到緊貼標記後面的獨立腳本。
- **跳掉**：冠軍資料回來後重寫 innerHTML，內容 5008→5656px，
  但 CSS 動畫不會重來，`translateX(-50%)` 換算基準一變就當場平移
  （實測 145px，最壞 324px）。改成動畫預設 `paused`，
  等內容確定才加 `.go`，所有重繪都發生在靜止狀態。

保險絲放在**上方**那支不依賴任何外部檔案的腳本，不放頁尾——
測試時發現 `shared.js` 沒載到會讓頁尾整行 boot 中斷，
保險絲放頁尾的話跑馬燈會永遠停住。

### 卡片區三頁移除「回主頁」、遊戲場與收藏冊頁首重排

手機直式從三排縮成兩排。連帶把橫幅左上的「護腎教室」文字改成一律顯示——
「回主頁」拿掉後，回衛教站首頁只剩那一個入口，再藏著文字說不過去。

遊戲場與收藏冊：腎元靠右獨立一排，四顆按鈕併成同一排（沿用卡片區的
`.hbreak`）。四顆一排放不下，所以拿掉收藏冊／回卡片區／遊戲場／已同步的
小圖示，「標準」的 🔠 保留。

### 首頁搜尋框加放大鏡

SVG 放在 `<input>` **之後**才能用 `input:focus ~ .sicon` 跟著換色；
`pointer-events:none` 才不會擋住點輸入框。左內距 16→44px。

---

## 2026-09-03

### 新增長文：拆解六種話術結構（系列三篇之三，系列完成）

`articles_src/no-dialysis-therapy-claims.md`，4,076 字、9 段、6 題 FAQ，
分類警訊與迷思。來源用 `kdigo_ckd` / `tsn` / `hpa_kidney`。**站上最長的一篇。**

作者指定要涵蓋的具體項目：EECP、中藥萃取物、不明點滴、護腎益生菌；
管道要涵蓋直銷、地下電台、電視廣告。作者也明確指示「著重話術結構是對的」。

寫作上的四個判斷：

**主體是結構，不是產品。** 產品會換，話術不會。逐一破解某某產品的文章
半年就過期，拆解結構的文章可以用很久——這也是唯一能處理「下一個沒聽過的
東西」的寫法。**全篇不點名任何產品、公司或個人。**

**開頭先承認腎功能確實會回升。** 急性腎損傷恢復、停掉傷腎藥、脫水矯正、
阻塞解除，四種情況都是真的。不承認這些，整篇的可信度就沒了，而且會被
拿去當成「醫師都說不可能」的反例。話術的高明處正是站在這些真實現象上面。

**三種管道分別對應不同的心理施力點**：直銷賣人際關係（拒絕的是關係不是
產品）、地下電台賣陪伴（真正被販賣的是被理解的感覺）、廣告賣專業感。
這段的用意是讓讀者理解為什麼「說那是騙人的」沒有用。

**EECP 寫成「真設備、正當適應症、但用在腎臟沒有證據」**，不寫成假貨——
它在頑固型心絞痛確實有角色，硬說它無效反而會被抓到把柄。同理，益生菌那段
承認腸道菌與尿毒素是真實的研究方向，只指出證據停在哪裡。

**作者定案的三處**：保留「如果真的有效，我們自己會先用」、保留 AI 合成
醫師影片、保留「為什麼沒有被取締」那題。**作者指示不要寫出腸道吸附劑的
藥名與試驗名稱**，維持在民眾看得懂的層次。

系列三篇至此完成。

### 新增長文：廔管為什麼要提前半年準備（系列三篇之二）

`articles_src/dialysis-access-preparation.md`，2,134 字、8 段、6 題 FAQ，
分類警訊與迷思。來源用 `kdigo_ckd` / `tsn` / `niddk`，與前一篇同一組。

標題選「臨時導管的代價比你想的大」而不是「透析管路怎麼準備」：**流程沒有人
會為了讀而讀，代價才會改變行為。**

寫作上的三個判斷：

**開場用兩個病人的對比，不用衛教口吻。** 從容扎針的人 vs 急診插管的人，
差別不是病情而是有沒有提前準備——這一句是整篇的論點，放在最前面。

**「保護那隻手」單獨成段，而且明說這段最實用。** 這是門診最少被講到、
病人自己最做得到的一件事：主動告訴每一個要抽血、打針、量血壓的人。
住院時尤其關鍵，因為接手的醫護不會知道。這段的價值高於任何通路比較表。

**明說導管有它必要的時候。** 急性腎損傷、廔管成熟前的過渡期，導管是正確
選擇。把問題定位在「因為沒準備而被迫使用」，而不是把導管講成壞東西——
否則已經在用導管的讀者只會覺得被責備，然後關掉頁面。

具體的週數一律寫成範圍（「數週到數月」），成熟時間個人差異太大，
寫死數字等於幫主治醫師做決定。

結尾三件事都寫成「今天就能做」：問一句話、決定保護哪隻手、住院時主動說。

### 上線：醫師說要準備洗腎了（系列三篇之一）｜`06ae0a4`

前一天的草稿轉正。加 `PAGE_SOURCES`、重建、OG 34 張、`bump_assets`
（`kg-8d2430fb16`）、`check_site` 全過。線上十一項驗證通過，
sitemap 31 個網址，搜尋索引 223 筆。

---

## 2026-09-02

### 新增長文草稿：三種腎臟替代療法怎麼選（系列三篇之一）

`articles_src/_kidney-replacement-therapy.md`，2,765 字、9 段、6 題 FAQ，
分類警訊與迷思。**草稿狀態，尚未上線。**

作者規劃的系列共三篇：本篇（尿毒症與替代療法）、提早準備透析管路、拆解
「免洗腎／逆轉腎療法」。標題由作者選定「醫師說要準備洗腎了」，比原案更貼近
病人真正的處境。

寫作上的三個判斷：

**「尿毒症不是一個數字」單獨成段。** 很多人以為 eGFR 掉到某個值就要洗腎，
但開始的時機看的是症狀與整體代償狀況，同樣 eGFR 8 的兩個人處置可以完全不同。
不先破除這個誤解，後面談選擇就沒有基礎。

**三種療法用表格並列，並明說「沒有哪一種比較高級」。** 腹膜透析常被誤會成
「血液透析做不了才選的備胎」，實際上長期存活數據相當。

**移植那段寫「不是治好，是換一種要照顧的東西」。** 終身免疫抑制劑、自行停藥
是移植腎失去功能的重要原因——這點比「移植最好」更需要被說清楚。也點出可以
在透析前先行移植，因為很多人是透析多年後才第一次聽到這個選項。

結尾銜接下一篇（血管通路需要數個月成熟），並在高齡族群那題納入「以症狀控制
為主的保守治療」也是需要被認真討論的選項，而不是放棄。

已驗證：草稿不產生頁面；暫時 publish 後十一項檢查全過並乾淨還原，
`published.json` 未被寫入。


### 修正桌機的 Facebook 分享鈕被誤藏

作者回報桌機看不到 Facebook。原因是我當初**拿 `navigator.share` 當「是不是
手機」的判準**——但 Windows 的桌機 Chrome／Edge 也有 Web Share API，於是桌機
被判成手機、Facebook 一起被藏掉。

判斷條件改成 `matchMedia('(pointer: coarse)')`（觸控裝置）。真正的原因本來就
只發生在手機：facebook.com 是 FB App 宣告的網域，OS 在載入網頁之前就把連結
交給 App。那是觸控裝置的行為，跟有沒有 Web Share API 無關。

另外加一個條件——**要同時有 `navigator.share` 才收起 Facebook**。否則在沒有
系統分享面板的觸控裝置上，等於把 Facebook 這條路完全拿掉、連替代方案都沒有。

`build_site.py`（18 個文章頁 + 首頁兩組）與 `shop.html`（頁尾那組）都改了。

DOM stub 補上 `matchMedia` 與三個新情境，其中 **B1「桌機有 navigator.share
但不是觸控」就是這次出錯的那一種**，現在有測試守著。兩支測試共 27 項全過。

### 全站移除使用者看得到的「商城」字樣

依作者指定：**知識商城 → 知識卡片**、**回商城 → 回卡片區**。其餘出現處沿用
同一套說法，語意不通的地方改寫整句而不是硬換兩個字（例如「第一名的名字會
出現在**商城**首頁的跑馬燈」→「出現在首頁的跑馬燈」）。

共改 19 處，涵蓋導覽列（25 頁共用）、首頁入口與區塊標題、商城／遊戲場／
收藏冊三頁的按鈕與內文，以及 `title` 提示文字。

**程式註解與 console 輸出刻意不動**（96 處）：那些「商城」指的是 `shop.html`
這個檔案，改成「卡片區」反而讓維護者對不上檔名。驗證時把 `<script>`、
`<style>`、註解排除後再掃，確認**使用者看得到的已經 0 處**。

替換腳本每一筆都用 `assert` 確認命中次數，改錯或漏改會當場停下來而不是默默
跑完——這種整站字串替換最怕的就是某處沒命中卻沒人發現。

**手機導覽列有個要留意的地方**：`.np` 前綴在手機隱藏，所以手機看到的是
「文章 · **圖卡** · 食物 · 計算 · 作者 · **卡片**」——圖卡與卡片相鄰且相似。
桌機顯示完整的「衛教圖卡 / 知識卡片」沒有這個問題。若要區隔，把 shop 的
`navlink` 標籤改成「收集」之類即可。

### 首頁加入全站搜尋（不含遊戲商城）

GitHub Pages 是純靜態、沒有後端可查詢，所以做成**建置時產生索引、前端比對**。
`build_site.py` 新增 `build_search_index()`，產出 `search_index.json`（216 筆、
225 KB），涵蓋 100 則知識卡、10 篇長文、101 張圖卡，以及計算／食物／簡介等
工具頁。**商城與遊戲不納入**——那是另一套內容與網址結構。

**索引在使用者第一次點搜尋框時才 fetch**，首頁初次開啟不會為了搜尋多下載
225 KB。

**中文用子字串比對，不用斷詞函式庫。** 這個資料量下斷詞帶來的準確度提升有限，
卻要多一個相依與數十 KB；子字串在中文上反而穩定，不會有斷錯詞導致搜不到。

排序：標題命中優先於內文命中（標題就是主題，內文可能只是順帶提到），**圖卡
再往後 40**——圖卡是配圖，同樣命中時文字內容才是完整答案。沒有這個權重時，
圖卡標題短、命中位置靠前，會把專門講那個主題的長文壓到第 5 名（實測過）。

順帶替每張圖卡加了 `id="g-<id>"` 錨點，搜尋才能直接跳到那一張而不是只到整頁。

**同時補上一個會靜默出錯的地方**：`sw.js` 對 `.json` 是快取優先，而
`search_index.json` 原本不在 `bump_assets.py` 的雜湊輸入裡——索引更新了版本號
不會變，回訪者會一直用到舊索引。已列入 `HASH_ONLY`，並實測「改動索引→版本號
跟著變→還原→版本號一致」。

驗證：216 筆的網址與錨點逐一比對檔案系統，0 項不存在；確認無商城網址、無空
內文；搜尋框只在首頁。實測 蛋白尿／止痛藥／香蕉 三種查詢，長文與知識卡都排在
圖卡之前，內文命中（香蕉→鉀）也有效。

## 2026-09-01

### 方針調整：以功能齊全為主，移除不準確的「不收集資料」文宣

作者指示不再糾結「不收集資料」，往後以功能齊全為優先，站上類似文宣順手
移除。這在事實面也是對的：**網站已有 Firebase 雲端同步與 Google 登入，
「不收集資料」早就不成立**，留著等於掛一個不準確的宣告。

清掉 `shop.html` 的 meta description 與 og:description（會出現在搜尋結果與
分享預覽）：「不收費、**不收集資料**」→「永久免費、不會要求信用卡」。同一句
還有另一個過期資訊，一併修正：**「60 張護腎知識卡」改為 100 張**。

**保留的部分**：頁尾的隱私說明本身是準確且完整的（載明 Firebase、Google 登入
會取得 email 與顯示名稱、可刪除雲端資料），那才是正確的揭露方式；誠實橫幅的
「永久免費」與頁尾的「不會要求信用卡」也都是事實，繼續留著。

全站掃過，除上述兩處外沒有其他「不收集資料／不會收集／不蒐集」類的文字。

### 手機橫放時把頁首按鈕壓到第二排

作者回報：直式已經是一排（**我先前的寬度估算是錯的，實際裝置為準**），但橫放
時寬度變大，整列會全部擠在同一排，又長又難按。

作法是在「標準」之前插一個寬度 100% 的零高度 flex 項目，把後面的按鈕推到
下一排。條件用 `(orientation:landscape) and (max-height:500px)`——**只看
orientation 不行**，桌機視窗拉寬也是 landscape，但它的高度遠大於 500px，
不該被這條規則影響。

瀏覽器面板這次恢復了，三種情境都實測過：

| 情境 | 斷行元素 | 結果 |
|---|---|---|
| 手機直式 375×812 | `none` | 不受影響 |
| 手機橫放 844×390 | `block` | 標準～登入 在第二排 ✓ |
| 桌機 1280×800 | `none` | 全部同一排，不受影響 |

### 商城分享圖示移到卡片區下方；頁首三顆按鈕拿掉小圖示

依作者指定，把社群圖示從頁首移到**卡片區與頁尾之間的深色區域**，置中一排。
搬動時整段標記原封不動搬過去，只換外層容器與樣式——JS 仍用
`querySelector('.hshare')`，不必改，也不會出現兩份會走鐘的標記。

「回主頁」「收藏冊」「登入／已同步」的小圖示已移除。

**但一排排不下，而且差很多。** 估算 390px 手機可用約 358px，六顆按鈕即使
拿掉那三個 emoji 仍需約 500px：

| 做法 | 寬度 | 結果 |
|---|---|---|
| 現在（已拿掉三個 emoji） | 500px | 超出 142px |
| 再拿掉「標準」的圖示 | 478px | 超出 120px |
| ＋文字縮短（主頁／收藏） | 451px | 超出 93px |
| ＋手機縮小內距與字級 | 390px | 仍超出 32px |

要真的一排，等於平均每顆只能 60px——**大部分按鈕都得改成純圖示**。這已經
超出「拿掉小圖示」的範圍，所以先停在這裡等作者決定。

### 商城頁首改放與衛教站相同的社群分享圖示

上一版誤解成一顆通用的「🔗 分享」，作者要的是**與首頁相同的那組社群圖示**。
已把那顆移除，改成五顆圖示（原生分享／LINE／Facebook／Threads／複製連結），
放在「已同步／登入」之後。

**SVG 路徑直接從 `build_site.py` 的 `ICONS` 取，不手抄**——手抄品牌字符抄錯
一個數字就是歪掉的 logo，而且不會有任何錯誤訊息。用一支注入腳本 import
`build_site` 再產生標記，兩邊必然一致。

**樣式用商城自己的色票**（`--ink`／`--card2`／`--edge`／`--brand`），沒有沿用
衛教站的 `--fg` 那套。混用會靜默失效——淺色模式退回繼承值看起來正常，深色
模式才爆掉。驗證確認這段 CSS 只用到那四個變數。

行為與衛教站一致：手機優先系統分享面板並隱藏 Facebook（App 會攔走並落到
動態牆），桌機保留 Facebook 並用彈出視窗開啟，彈窗被擋則不阻止預設行為；
複製失敗退回 `execCommand`，回饋是鏈結圖示換成打勾。

驗證：抽出 IIFE 用 DOM stub 跑三種情境共 13 項全過。

### 商城移除「加入主畫面」提示條，頁首改放分享按鈕

移除的是整組東西，不是只藏起來：`.installbar` 的標記、CSS（含 `.ibtn`）、
以及 `beforeinstallprompt` 那整段 `installHint` JS 都刪掉了，殘留檢查全為 0。
`localStorage.msd_noinstall` 不再被寫入，舊使用者身上留著也無害。

頁首那一列（腎元／標準／回主頁…）在「回主頁」之後加了 **🔗 分享**。用 emoji
而不是 SVG，是為了跟同一列其他按鈕的作法一致，也不必新增 CSS。

行為：手機優先叫系統分享面板（能直接開 LINE 等已安裝的 App），桌機沒有這個
API 就退回複製連結，`clipboard` 失敗再退回 `execCommand`。**三條路都給回饋**
（按鈕文字換成「✅ 已複製」），否則使用者會以為按了沒反應。

驗證：抽出那段 IIFE 用 DOM stub 實跑三種情境共 6 項全過。商城是手寫頁、沒有
產生器把關，更需要實際執行過再推。

### 首頁加入兩組分享圖示

位置：**「這個網站怎麼用」方塊下方**，以及**免責聲明下方**（頁尾之前）。
兩組都是純圖示，沒有標題與說明文字。分享時帶的標題與首頁 h1 一致。

實作上有個要小心的地方：免責聲明在 `page()` 這個**共用骨架**裡，直接加在那
會讓每一頁都多一組——文章頁本來就有兩組，會變成三組。所以替 `page()` 加了
`after_disclaimer` 參數，只有首頁傳值。驗證特地檢查了「其他頁面沒有被多加
一組」，文章頁確認仍是兩組。

腳本一樣只輸出一份，掛在免責聲明下方那組（文件較後面，執行時兩組都已存在）。

### 分享按鈕改為純圖示、固定一排，並在文章頂端也放一組

**為什麼拿掉文字**：帶文字的四顆在 375px 手機上約需 445px，而可用寬度只有
約 335px，**排不成一排**。圖示版每顆 44px、四顆加間距約 206px，綽綽有餘。
語意用 `aria-label` 與 `title` 保留，讀螢幕軟體與滑鼠停留都讀得到。
`flex-wrap:nowrap` 是刻意的——這排永遠不該換行。

頂端那組放在簡介與目錄之間，沒有標題也沒有說明文字，純粹四顆圖示。

**改版時最容易出錯的地方**：原本腳本用 `document.querySelector('.share')`
只抓第一組。拆成兩組之後若沒改，**頂端那組會完全沒有反應且不會有任何錯誤
訊息**。改成 `querySelectorAll` 綁定所有 `.share`，腳本仍只輸出一份（放在
文末那組，執行時兩組都已存在）。DOM stub 特地測「頂端那組的分享也能用」。

複製的回饋也跟著改：純圖示按鈕沒有文字可換，改成把鏈結圖示換成打勾
（`.i-off` / `.i-on` 互換），並補 `.sb svg[hidden]` 的 CSS。

驗證：18 頁各兩組、腳本各一份、按鈕各五顆、`aria-label` 與 `title` 各五個，
共 0 項問題；DOM stub 13 項行為檢查全過。過程中修了檢查腳本自己的兩個誤判
——「本頁內容」四個字在 `<style>` 註解裡也有（用它比對順序會誤判），以及
`data-title=` 被算進 `title=` 的計數。

### 手機版移除 Facebook 分享鈕；修掉 hidden 被 CSS 蓋掉的 bug

作者實測回報：手機上 LINE 與 Threads 都正常，**只有 Facebook 會落到動態牆**。

**Facebook App 的攔截無法從網頁端繞過。** facebook.com 是 FB App 宣告擁有的
網域，Android 的 App Links／iOS 的 Universal Links 會在載入網頁「之前」就把
連結交給 App，而 App 不認得 `sharer.php` 的路徑。這個決定在作業系統層，
`window.open`、`target`、`rel` 都影響不了。改用 `m.facebook.com` 或透過自家
網域轉址也沒用——最後一步還是走到 facebook.com。就算真的強迫用瀏覽器開，
多數人手機瀏覽器並沒有登入 Facebook（他們登入在 App 裡），只會看到登入頁。

依作者指示，**偵測到 `navigator.share` 時（即手機）隱藏 Facebook 那顆**。
第一顆「分享」走系統面板，選 Facebook 會開 App 自己的原生發文框，是行動
裝置上正常運作的路徑。LINE 與 Threads 實測正常，保留。

**同時修掉一個自己寫的 bug**：`.sb` 設了 `display:inline-flex`，而 `hidden`
屬性是靠瀏覽器預設樣式的 `display:none` 生效的——**作者樣式優先於瀏覽器預設
樣式，所以 hidden 被蓋掉了**。也就是說桌機上那顆「分享」一直是看得見的，但
按下去完全沒反應（桌機沒有 navigator.share，事件根本沒掛上）。補
`.sb[hidden]{display:none}` 解決。作者在手機測所以沒遇到。

DOM stub 加了「手機隱藏 Facebook」與「桌機保留 Facebook」兩項，共 15 項行為
檢查全過。

### 修正 Facebook 分享跳到動態牆而不是發文框

回報：按 Facebook 分享後跳到 Facebook，但沒有進入貼文畫面。

先排除頁面問題——**OG 標籤完整、Facebook 爬蟲抓得到（HTTP 200）、分享連結
的參數編碼也正確**，所以不是頁面或網址的問題。Facebook 擋掉自動化請求
（連線被強制關閉），無法直接驗證它回什麼，以下是依據已知行為的判斷。

`sharer.php` 原本就是設計成**用彈出視窗**開啟的。當成一般整頁跳轉時，尤其在
手機上或被 Facebook App 攔截時，常常就落到動態牆而不是發文框。

兩個修正：

1. **品牌連結改用彈出視窗開啟**（600×540），這是那些分享頁被設計的用法。
   彈窗被擋時不阻止預設行為，讓 `<a>` 照原本方式開啟，不會變成按了沒反應。
   `window.open` 不吃 `rel=noopener`，所以另外把 `w.opener` 設為 null。
2. **原生分享按鈕移到第一顆並改名為「分享」**。手機上這是最可靠的路徑——
   直接叫出已安裝的 App，不會被網頁版分享頁的攔截問題影響。原本它排最後
   又叫「其他…」，等於把最好用的選項藏起來。

DOM stub 加了兩個情境：彈窗可用（要用 window.open 且阻止跳轉）與彈窗被擋
（不可阻止跳轉），共 13 項行為檢查全過。

註：這次沒有 bump `sw.js`——`sw.js` 對 HTML 是連線優先，文章頁更新會立即
送達，不需要靠版本號；快取優先的只有圖片與 js/css，而那些沒動。

### 文章頁加入轉發按鈕

18 個頁面（8 個分類頁 + 10 篇長文）文末加入分享區塊，位置在參考來源之後、
延伸閱讀之前——讀完的那一刻才是想轉發的時機。

**用純連結的分享網址，不掛 Facebook／LINE 官方外掛。** 理由是功能面：外掛
能多做的只有顯示分享次數，卻要多載入數十 KB 的第三方腳本、拖慢頁面。連結版
功能一樣完整，手機上的原生分享面板涵蓋得還更廣。

LINE 放第一個：受眾是民眾與病患，在台灣把衛教轉給家人最常用的就是 LINE。
另有 Facebook、Threads、複製連結，以及手機上的系統原生分享面板
（`navigator.share`）——它能叫出所有已安裝的 App，涵蓋範圍比固定幾顆按鈕大，
不支援的瀏覽器就不顯示那顆。

樣式用外框而非填色：文末已經有參考來源與延伸閱讀，再加一排實心色塊會變成
廣告帶。手指目標維持 44px 高。

**圖示**：品牌那三個用各平台官方的字符外形（取自 simple-icons，圖示資料以
CC0 釋出；商標權仍屬各品牌，此處用途是「連向該平台的分享按鈕」，屬各平台
品牌規範允許的用法）。**刻意不自己畫近似品——畫歪的 logo 比純文字更糟。**
複製與分享那兩顆不是品牌標誌，自己畫線條圖示。全部內嵌成 SVG，執行時不對外
發任何請求。

加圖示時順帶修掉一個會讓圖示消失的 bug：複製按鈕原本用 `button.textContent`
換成「已複製」，按鈕裡有 SVG 之後那樣會把圖示一起清掉、而且不會再回來。
改成把文字包進 `<span>` 只換 span 的內容，DOM stub 也加了「圖示是否還在」的
斷言。

**驗證方式（瀏覽器面板當時卡住，改用更強的做法）**：把內嵌 JS 抽出來丟給
`node --check` 確認語法合法——f-string 裡寫 JS 最容易在大括號跳脫出錯，產生
的頁面照常顯示但按鈕沒反應且沒有任何錯誤訊息；再用最小 DOM stub 實際執行，
涵蓋三種環境：手機（有 navigator.share）、桌機（沒有）、舊瀏覽器（沒有
clipboard API 要退回 execCommand），共 9 項行為檢查全數通過。

過程中修了兩個測試本身的錯：Node 24 內建唯讀的 `navigator` 全域，用
`global.navigator =` 覆蓋會失效，改成用 `new Function` 把環境當參數注入；
以及 clipboard 是非同步的，同步斷言會得到假失敗。

### 新增長文草稿：護腎的生活習慣（八個分類的收尾）

`articles_src/_kidney-lifestyle-evidence.md`，2,944 字、8 段、6 題 FAQ，
分類生活習慣。**草稿狀態，尚未上線。**

這篇的角色與前八篇不同：前面都是單一主題深入，這篇把水分、運動、睡眠、
戒菸、體重、保健食品放在一起**做取捨排序**。前提是「**自制力是有限的資源，
與其樣樣做一點，不如把力氣放在真正有用的地方**」。

明確區分「有證據」與「聽起來很養生」，並附一張證據強度對照表。**戒菸排
第一**——它是最被低估的一項，而且是唯一「完全戒除就能拿回大部分效益」的。

「多喝水」單獨拿出來寫，因為它被誤解得最深：**腎臟不是水管，沖不掉東西**，
沒有證據支持大量飲水能減緩 CKD 進展，而心衰竭、肝硬化、進展期 CKD 與透析
病人喝過量是有害的。正確說法是「喝足夠」而非「多喝」。

寫作上的一個判斷：初稿沒有表格（用 H3 分項），測試時抓到。這篇的核心正是
排序與取捨，補一張對照表是內容本身該有的，不是為了通過測試。

`PAGE_SOURCES` 用 kdigo_ckd／hpa_kidney／niddk，與生活習慣分類頁一致。

已驗證：草稿不產生頁面；暫時 publish 後十一項全過。特別確認 **FAQ 只抓到
6 題**——本文有 13 個 H3，抽取器正確地只掃「常見問題」區段。

### 新增長文草稿：膽固醇報告怎麼看

`articles_src/_cholesterol-report-ckd.md`，2,940 字、9 段、6 題 FAQ，
分類血脂代謝。**草稿狀態，尚未上線。**

血脂是三高裡最容易被輕忽的——沒有症狀、數字又多到看不懂。這篇的重點
不是背標準值，而是「**知道自己屬於哪一種人**」。

三個核心概念：**非 HDL 可以自己算**（總膽固醇減 HDL），在三酸甘油酯偏高
時比 LDL 更能反映風險；**Lp(a) 一生驗一次就夠**，因為由基因決定、飲食運動
改變不了；以及**不必為了驗血脂而空腹**。

慢性腎臟病的特殊性佔了最大篇幅，四點：血脂型態不同（TG 高、HDL 低、LDL
不一定高，只看 LDL 會誤判「我正常」）；CKD 本身就是心血管高風險；治療
邏輯偏向依風險決定而非追 LDL 數字；以及**已在透析者一般不建議新開始
statin**——這一點反直覺且網路上查不到，特別寫明是因為「醫師的處置和你查到
的一般人版本不同，不代表漏掉了」。

也處理了「statin 傷腎嗎」——不會，因為怕傷腎而拒絕的代價通常大於風險。

`PAGE_SOURCES` 用 kdigo_lipid／aha_lipid／tsoc。AHA 2026 版已改名為
Dyslipidemia 並納入 Lp(a) 與殘餘顆粒，與本文主軸正好一致。

已驗證：草稿不產生頁面；暫時 publish 後十一項檢查全過並乾淨還原。

### 新增長文草稿：居家血壓怎麼量才算數

`articles_src/_home-blood-pressure-measurement.md`，2,719 字、10 段、6 題 FAQ，
分類血壓管理。**草稿狀態，尚未上線。**

切入點是「**量錯血壓，後面的治療全部會跟著走偏**」——該治療的沒治療、
不需要加藥的被加藥。而這是整條路徑裡少數完全由病人自己掌控的環節。

把知識卡裡分散的 722、袖帶尺寸、白袍與隱匿性高血壓、姿勢性低血壓串成
一套可執行的作法。特別展開兩個實際會改變數字卻常被忽略的細節：**袖帶太小
會高估、太大會低估**；以及「休息五分鐘」與「量的時候不要說話」。

「最常見的七個量錯方式」是全文最實用的一段，其中「量到滿意的數字才記錄」
和「只有不舒服才量」是門診真正會遇到、但衛教文章很少寫的行為問題。

數字判讀寫明台灣 2022 年已下修為 130/80 且以居家 722 為基準，但同時強調
**目標值因人而異、判讀的是平均不是單日**，避免讀者拿別人的目標套自己。

`PAGE_SOURCES` 用 kdigo_bp／aha_bp／tsoc——722 出自心臟學會體系，比國健署
的三高衛教頁更貼題。

已驗證：草稿不產生頁面；暫時 publish 後十一項檢查全過並乾淨還原。

### 新增長文草稿：腎功能還能不能恢復

`articles_src/_kidney-function-recovery.md`，2,748 字、9 段、6 題 FAQ，
分類警訊與迷思。**草稿狀態，尚未上線。**

同時處理門診兩個方向相反的誤解：看到一次紅字就以為要洗腎（急性的往往
可逆），以及「反正都壞了控制也沒用」而放棄回診。

寫作上的分寸：**慢性腎臟病的部分沒有給假希望。** 明講人類腎絲球出生後
不會再增加、失去的不會長回來，再把目標轉到「速度可以改變」——並說明
壓回接近自然老化的速度，對很多人就等於一輩子不必透析。給希望但不騙人。

另外納入一個常被忽略的觀念：**急性腎損傷即使數字完全恢復，日後發展成
慢性腎臟病的風險仍升高**，所以不該就此結案。也澄清急性期的透析多為暫時
性，與末期的長期透析是兩回事——這兩者在口耳相傳中經常混為一談。

`PAGE_SOURCES` 加 kdigo_ckd／tsn／hpa_kidney，與警訊與迷思分類頁一致。

已驗證：草稿不產生頁面；暫時 publish 後十一項檢查全過並乾淨還原。

### 新增長文草稿：糖尿病腎病變

`articles_src/_diabetes-kidney-disease.md`，2,922 字、9 段、6 題 FAQ，分類血糖管理。
**草稿狀態，尚未上線。**

選題理由：血糖管理是長文缺口最大的分類，而糖尿病是台灣洗腎最主要的原因。

主軸是「**它最該處理的階段，剛好是你完全沒有感覺的階段**」。展開四階段
自然病程（過度過濾 → 微量白蛋白尿 → 明顯蛋白尿 → eGFR 下降），重點放在
**eGFR 往往是最後才變的那一個**，只看抽血會錯過前三個階段；同時說明有一
部分人的病程不照這個順序，所以 UACR 與 eGFR 兩項都要驗。

藥物只寫到「有哪些方向、為什麼現在和十年前不同」，**不寫劑量、不指定用藥**，
明確交回主治醫師判斷。破解「不吃西藥腎臟就不會壞」時把因果講清楚：傷腎的
是失控的血糖血壓，不是控制它們的藥。

`PAGE_SOURCES` 同步加上 kdigo_dm／ada_soc／endo_dm。

已驗證：草稿不產生頁面；暫時 publish 後十一項檢查全過並乾淨還原，
`published.json` 未被寫入。

### 新增長文草稿：止痛藥與腎臟（NSAID）

`articles_src/_painkiller-nsaid-kidney.md`，2,973 字、10 段、6 題 FAQ。
**檔名以底線開頭＝草稿，尚未上線**，等作者審閱後把底線拿掉即可發布。

選這個題目的理由：現有四篇長文有三篇是「檢查數值」、一篇「飲食護腎」，
**用藥安全掛零**，而它同時是搜尋量最大、可介入性最高的題目。

內容主軸是「條件」而不是「禁令」——止痛藥傷腎是真的，但不是每個人每次
吃都會出事。特別展開三重打擊（NSAID ＋ 利尿劑 ＋ ACEI／ARB）、乙醯胺酚
與 NSAID 的機轉差異、外用製劑不是零吸收，以及台灣特有的來路不明藥物與
中草藥問題。也刻意寫了「忍痛不是好選項」——門診第二常見的錯誤方向。

順帶補上 `PAGE_SOURCES` 的對應條目。原本漏了，publish 後會沒有參考來源
區塊，與其他四篇長文不一致。

已驗證：草稿狀態不產生頁面；暫時 publish 後十一項檢查全過（分類連結、
表格、FAQ 六題結構化資料、參考來源、署名、免責聲明、sitemap、og:image），
並乾淨還原，`published.json` 未被測試寫入。

## 2026-08-31

### 文章頁新增「專家訪談」區塊

未來收到專家文稿就放進 `interviews.json`，不必再動程式。

**沒有稿子就完全不輸出，不留空占位。** 醫療衛教站掛一個空的「專家訪談」標題，
讀者看到的是「這站沒做完」，對 YMYL 內容是扣分而不是加分。

鍵與 `PAGE_SOURCES` 相同：八個分類頁用分類名，四篇長文用 slug。**鍵以底線
開頭 = 草稿**，不會上線，與 `articles_src` 的規則一致——醫療內容掛別人的名字
發布前必須先取得對方同意，需要一個「能寫進檔案但不會上線」的狀態。

結構化資料用 `mentions` 指出受訪者。schema.org **沒有 Interview 型別**，硬套一個
不存在的型別只會讓驗證器報錯，`mentions` 是能誠實表達的最貼近描述。

`check_site.py` 加了第 8 項檢查鍵名。鍵名打錯的話訪談稿就是靜靜地不顯示——
沒有錯誤、沒有空區塊，只有「我明明寫了怎麼沒出現」，正是這個專案一再遇到的
那類問題。

**刊出前**：取得受訪者書面同意（姓名、職稱、內容的使用範圍）；受訪者若為
執業醫師，內容不要出現招攬或指名看診的字句（醫療法 87 條）。

### 新增 `check_site.py`｜`ce472a4`

一天內踩到三次同一種模式：**產生器與它產生的資產各自演進、沒有人對帳**。
出錯時沒有例外、沒有破圖，只有使用者看到舊的或錯的東西。把每次事故變成
一項自動檢查：

1. 卡片資料兩份是否同步 — `data.js` 與 `knowledge_export.json` 沒有同步機制
2. 卡片插圖與產出是否齊全 — 缺插圖會靜默退回程式繪製的後備圖
3. HTML 引用的本地檔案 — 路徑打錯只會變成破圖
4. CSS 變數有無用了沒定義的 — 衛教站誤用商城的 `--ink` 會靜默失效
5. 手機字級是否被接管 — 少了 `text-size-adjust`，CSS 字級形同虛設
6. 快取版本號是否最新 — 圖片快取優先又不帶 `?v=`，沒 bump 就永遠是舊圖
7. 產生器的來源檔 — 來源在 repo 外，消失或過期要及早知道

`data.js` 用 node 實際執行取值，不用正則硬拆——**拆錯的檢查比沒有檢查更糟**。
第 6 項重用 `bump_assets.sw_version()`（本次從 `bump_sw` 抽出）而不是另寫一份，
兩份實作遲早走鐘，而走鐘正是那個版本號要防的錯。

第 4 項只查**沒有預設值**的 `var(--x)`：`var(--fs,1)` 是刻意寫法，變數由 JS 在
執行期設定，預設值就是平常生效的值。`dash.html` 是站長私用頁，沿用專案既有界線排除。

已逐項注入故障驗證七項都抓得到並正確還原。**只會回報「通過」的檢查器等於沒有檢查器。**

### `make_logo.py` 的來源改指 .NET 版標誌｜`384cf3e`

`SRC` 一直停在 `知識卡插圖/護腎教室.jpg`，那張的字是改網域之前的
**KIDNEYGOD.STUDIO**。2026-08-27 換成 .NET 版時 `logo.png` 是直接放進倉庫的，
腳本沒跟著更新——**只要有人跑一次 `make_logo.py`，頁首標誌就會被悄悄換回一個
不存在的網域，而且沒有任何錯誤訊息**。

改指 `Downloads/kidneygod.png`，`MAX_W` 從 480 調成 684 與現有同寬（維持 480
重跑會把標誌縮小變糊）。沒有覆蓋現有的 `logo.png`——它是正確的，重新產生對
使用者沒有差別，不動正在正常運作的資產。

### 分享預覽圖全站改用 logo｜`27d41e2`

依指定，26 頁共用同一張。檔名維持一頁一個，所以沒動到任何 HTML 的 `og:image`，
將來要改回一頁一張也不必再改 `build_site.py`。

**logo 只放在中央正方形安全區內（580px 寬），不橫跨滿版。** 各家裁法不同：
FB／Threads 約 1.91:1、Twitter 2:1、LINE 與部分動態牆接近 1:1，滿版的 logo 遇到
正方形裁切會被切掉「護」和「.NET」。實測 1.91:1、2:1、1.33:1、1:1、4:5 五種
裁切都完整。代價是 1.91:1 時留白較多——但那是白底本身的顏色，看起來像刻意
留白而不是破圖。

### 補上第二批 40 張知識卡插圖｜`66778a6`

100 張知識卡從此全部使用手繪插圖。

**檔案編號跟卡片順序並不一致**（`61.jpg` 是腳踝水腫，但卡片陣列第 61 張是血壓計
袖帶）。對應關係取自 `GI插圖_新增40張_Prompt.txt` 每段標示的檔名，並用三個獨立
證據驗證：缺插圖的 40 張正好等於該 prompt 的 40 筆且與現有 60 張零重疊；
`61.jpg`→`bp-ccb-edema`、`100.jpg`→`shenkui` 圖文相符；產生器回報 100 張全部
套用外部插圖。

順帶修掉一個會靜默出錯的問題：`bump_assets.py` 的雜湊**沒有涵蓋 `cards/gi/`**，
換了插圖不會換 `VERSION`，看過那張卡的人會永遠停在舊圖。新增 `HASH_DIRS`，
目錄採排序後逐檔雜湊以保持冪等。

### 修正手機上誠實橫幅被撐成兩行｜`8bca487`

橫放時橫幅文字比旁邊按鈕還大又折成兩行。**根因不是字級設太大**——`shop.html`
與 `library.html` 漏了 `text-size-adjust`，Android Chrome 的自動字體放大把整段
文字撐大了，CSS 裡的字級從頭到尾都是 13px。

商城橫幅文案依指定縮短為「知識卡片收集遊戲 — 不收費・不會要求信用卡」，直式
375px 也能一行且不必縮字級。移除的「唯一會出貨的是護腎知識」在跑馬燈、頁尾
說明與 meta description 都仍在（法遵訴求沒有消失）。

品牌標誌對齊：加一層 `.hnwrap` 沿用各頁 header 的 `max-width` 與內距（商城／
收藏冊 1180px、遊戲場 1000px，**三頁數值不同**）；`align-items` 改 `flex-start`
讓標誌貼齊上緣。SVG 圖形在畫布裡左邊留了 3.5px 空白而 `logo.png` 只有 0.5px，
故補 `margin-left:-3px` 做光學對齊——**外框對齊不等於看起來對齊**。

### 商城三頁的誠實橫幅加上衛教站首頁連結｜`ed59e58`

標誌用衛教站深色模式的主色 `#5eead4` 而非淺色版的 `#0f766e`：誠實橫幅是深色底，
淺色版對比只有 3.56，深色版 13.18。窄螢幕隱藏文字只留標誌，量測確認桌機與手機
的橫幅高度都與改動前相同，誠實文字逐字未變。

### 12 篇衛教頁文末加入權威來源引用｜`1dfb60a`

八個分類頁與四篇長文各附三條，同時寫入 schema.org 的 `citation`。來源集中在
`build_site.py` 的 `SOURCES` 與 `PAGE_SOURCES`，**網址逐條連過確認標題正確且未被
導轉**；一律連學會官方指引頁而非期刊 DOI——全文多半擋機器人或需付費，且指引
改版時官方頁會跟著更新。

過程中查到 `國健署 EBook.aspx?nodeid=1157`（慢性腎臟病健康管理手冊）已失效，
會 302 導回首頁，網路上仍在流傳這條連結。改用 `List.aspx?nodeid=217`。

---

## 已知的坑（改東西前掃一眼）

- **`<img>` 的 `width`/`height` 屬性會蓋掉 CSS 的 `aspect-ratio`**：那兩個屬性會變成 CSS 呈現屬性（`height:900px`），只覆寫 `width:100%` 的話比例完全失效。凡是搭配 `aspect-ratio` 的 `<img>` 都要寫 `height:auto`。2026-09-04 首頁封面因此變成 679×900 的巨大方塊——**而且放圖之前看不出來**，當時全是沒有 height 屬性的 `<div>` 佔位塊。
- **頁首可用寬度是 `.wrap` 的 `--maxw`（720px）扣掉內距，和螢幕多寬無關**：導覽列排不下時，桌機不會因為視窗變寬而變好（實測 960px 一樣兩排）。加項目一定要重掃 320–1400 各寬度。
- **`make_logo.py` 預設不覆寫 `logo.png`**：倉庫裡那份是手動放的，與腳本算出來的不同（684×370 vs 684×375）。只是想產別的東西時跑一下，會把全站標誌悄悄換掉。要重產請加 `--logo`。
- **`svg.hidden = true/false` 對 SVG 元素無效**：`hidden` 是 `HTMLElement` 的屬性，`SVGElement` 沒有，設了不會噴錯也不會生效。主題切換的三顆圖示因此全部藏著。要用 `setAttribute('hidden','')`／`removeAttribute('hidden')`。
- **`:root[data-theme="dark"] a{}` 的權重是 (0,1,1)，會壓過 `.sb{}` 的 (0,1,0)**：深色模式的分享按鈕因此變成連結色（藍綠）。凡是要讓元件自己決定顏色的，主題層只改 `--link` 這種自訂屬性，不要直接寫元素選擇器。
- **`minmax(220px,1fr)` 在容器窄於 220px 時會溢出**（特大字級下很容易發生），要寫 `minmax(min(220px,100%),1fr)`；而 `1fr` 的最小值是 `auto`，圖片不會縮，要用 `minmax(0,1fr)`。
- **`data.js` 與 `knowledge_export.json` 是兩份不會互相同步的複本**，改文字兩邊都要改。`check_site.py` 第 1 項會擋。
- **衛教站用 `--fg`／`--bg`／`--card`／`--mut`，商城自成一套用 `--ink`**，混用會靜默失效——淺色模式退回繼承值看起來正常，深色模式才爆掉。
- **圖片是快取優先且網址不帶 `?v=`**，換圖必跑 `bump_assets.py`，否則看過的人永遠是舊圖。
- **`.nojekyll` 是必要的**，沒有它 GitHub Pages 會跑 Jekyll 然後建置失敗。
- **`header` 的 `top:34px` 是寫死的**，誠實橫幅實際高 35.8px，有 1.4px 重疊。因橫幅 `z-index` 較高而看不出來，暫不處理。
- **社群平台自己會快取預覽圖**，換了 `og/*.jpg` 之後既有貼文不會自動換圖，要用各平台的分享偵錯工具重新抓取。
- **沒有任何排程在跑 `build_site.py`**（2026-08-31 查證 189 個排程）。內容改完要自己跑。
