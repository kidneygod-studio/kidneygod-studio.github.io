__pycache__/
*.pyc

# macOS 的資料夾檢視設定。開發環境從 Windows 換到 Mac 之後才開始出現，
# 每開一次 Finder 視窗就冒一個，沒擋的話遲早混進某次 commit。
.DS_Store

# 長文大圖的原圖（2752x1536、共 26 MB）。網站只需要 make_hero.py 壓過的那份，
# 原圖進了 git 歷史就永遠拿不掉。要換圖時把新原圖放進去重跑 make_hero.py 即可。
#
# 現在原圖實際放在雲端（見 asset_paths.hero_src_dir），repo 內通常根本沒有
# 這個目錄。但它仍可能以實體資料夾或符號連結的形式出現在某些機器上。
#
# ⚠ 不要加尾斜線。git 把符號連結當檔案、不當目錄——寫成 hero_src/ 擋不住
# 符號連結，連結本身就會被 commit 進去，而它的目標路徑每台機器都不一樣。
# 沒有尾斜線則兩種形式都能匹配。
hero_src

# 血液透析中心那個站的原圖，理由同上。壓過的 dialysis/img/ 才進版控，
# 換圖時把新原圖丟回去重跑 make_dialysis_img.py。
dialysis/img_src

# 原始素材的單機備援位置（asset_paths.py 的最後一個候選）。正常情況下素材
# 放在雲端同步資料夾，這裡只是沒有雲端時的退路——同樣不能進版控。
assets_src

# 匯入時被期刊篩子刷掉的、以及過濾前的備份。想改主意時看這兩份，
# 不需要進版控。
articles_src/news_dropped.json
articles_src/news_before_filter.json
